# ── 凭据与运行时数据（绝不入库）──
credentials.json
credentials.local.json
auth_cookie.txt
auth_cookie.txt.bak
notify-config.json
*.sqlite3
*.sqlite3-journal
monitor.log
data/
data/img-cache/
# events 插件 config.json（可存 Google API key 兜底，防 git add . 泄 key，走 DB/env 优先）
plugins/**/config.json
api_calendar_config.json

# ── 环境与运行时 ──
.env
.env.*
!.env.example
# 两种形态都要挡：目录（node_modules/）与符号链接/文件（node_modules）。
# 带斜杠的模式只匹配目录，贡献者容器里的 node_modules 符号链接曾被 git add -A 混入并推到 main
# （2026-09-22 清理：移除误提交的 node_modules 符号链接）
node_modules/
node_modules
__pycache__/
*.pyc
service-logs/
# ── 日志模块产物（默认 LOG_DIR 落项目内 logs/）──
logs/
# ── Hermes 插件本地文件 ──
.hermes/
workspace/

# ── 插件本地文件（用户私有插件，不入库）──
plugins/local/*
!plugins/local/.gitkeep

# ── Qoder 工具缓存（repowiki/knowledge 为生成物，已发布到 GitHub wiki，不入库）──
.qoder/

# ── 编辑器/系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
api-test-results.json
downloads/
*.bak
*.bak-*
backup/
backups/
*.sqlite3-wal
*.sqlite3-shm

# ── 收藏 PDF 生成缓存与产物 ──
img_cache/
pdf/
/tmp/

# 前端构建产物出库（issue #186 方案 A）：安装期/镜像构建生成，禁止提交
plugins/official/web-dashboard/ui/dist/
