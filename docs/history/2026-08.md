# 演进记录 — 2026-08

> 本文件是 vrchat-assistant 的月度演进记录。每条记录对应一次发布 / 一个 PR / 一个关键 commit，
> 附带"演进意义"——说明为什么做这件事（commit log 里没有的上下文）。
> 阅读指南见 [INDEX.md](./INDEX.md)。

## 2026-08-07 · v1.0.0 — 首个正式版

**发布**：`v1.0.0` — VRChat 好友监控系统首个正式版

**里程碑意义**：项目诞生。从 VRCX-0 的痛点出发（GUI 臃肿 / WebSocket 断线不重连），自建实时好友监控系统：Node.js + SQLite(WAL) + WebSocket + MCP + Hermes 插件托管。

**关键 commit**：
| hash | 类型 | 内容 |
|------|------|------|
| ead184b | feat | VRChat 好友监控系统 v1.0.0 |
| 113-111 | feat/fix | 桌面插件配置面板、分隔线主题变量、插件精简为账号配置弹窗 |
| fad0e3a | fix | 朋友 AI 审查发现的 3 个问题 |
| 5bf7523 | docs | 完善 AGENTS.md 配置引导 + 修正 README |

**演进意义**：确立技术栈（better-sqlite3 前身 sql.js 仍在用）与"AI Agent 优先"的项目定位雏形。

## 2026-08-07 · v1.1.0 — 邮箱通用化

**发布**：`v1.1.0` — 邮箱通用化 + OTP 文件夹遍历修复

- `58b8642` feat: 邮箱通用化 — 支持任意 IMAP 邮箱（不再绑定 QQ）
- `4a8fa8b` fix: fetch-otp.py 文件夹遍历兜底 + AGENTS.md 补 MCP 配置

**演进意义**：登录验证码抓取从"QQ 专用"泛化为任意 IMAP，扩大潜在用户面。

## 2026-08-07 · v1.2.0 — 迁移脚本参数化

**发布**：`v1.2.0` — 迁移脚本参数化

- `be204be` feat: 迁移脚本参数化 — 支持任意用户的 VRCX 数据库
- `99-97` feat: get_online_pattern 上线规律分析 / 昵称映射落库 + get_nicknames/set_nickname

**演进意义**：VRCX-0 历史数据迁移（10 个月数据是用户的刚需）从写死路径泛化为通用工具；上线规律分析是第一个"分析型"工具。

## 2026-08-07 · v1.5.0 — Agent Skill 开箱即用

**发布**：`v1.5.0` — 开箱即用 Agent Skill + 敏感信息清理

- `2b72b06` docs: 新增开箱即用的 Agent skill + 敏感信息清理
- `d20dc17` docs: 写操作工具功能说明补全

**演进意义**：确立"skill 即文档"模式——给 AI Agent 用的技能文档随仓库发布，是后来 skills/ 目录的开端。

## 2026-08-07 · v1.6.0 — 社交操作套件

**发布**：`v1.6.0` — 社交操作套件（加好友/删好友/共同好友）

- `e103b72` feat: send_friend_request 添加好友工具
- `8c635b0` feat: remove_friend 删除好友工具（confirm 安全护栏）
- `3306307` feat: get_mutual_friends 共同好友列表工具

**演进意义**：从"只读监控"跨入"写操作"——社交互动成为能力域，confirm 护栏是先例（后续写操作均带确认机制）。

## 2026-08-07 · v1.7.0 — 媒体套件

**发布**：`v1.7.0` — 媒体套件（emoji/相册/图库 上传下载管理）

- `90-86` feat: get_boop_emojis / upload_emoji + emoji 方形化预处理 + 照片上传（相册+图库）+ 下载工具 + 列表/删除工具
- `b59d357` chore: downloads/ 加入 gitignore
- `6b9050c` docs: 公共 skill 同步更新（工具数 18→27，补列表/删除/下载工具）

**演进意义**：媒体管理能力域成型（戳戳 emoji / VRC+ 相册 / 图库的完整读写闭环）；工具数从 18 涨到 27+，MCP 工具矩阵开始膨胀。

## 2026-08-08 · v1.8.0 — 群组查询

**发布**：`v1.8.0` — 群组查询工具 & 在线好友增强

- `fea0c62` feat: 4 个群组查询 MCP 工具（get_user_groups/get_group_info/get_group_instances/get_group_announcement）
- `2593da3` enhance: get_online_friends 增加 nickname + locationParsed（向后兼容）
- `686e263` fix: parseLocation 处理 ~local 实例（无括号被误判为 public）
- `81-80` fix: emoji 上传显式 contentType

**演进意义**：群组能力域起步；确立"向后兼容增强"与"解析边界修复"的两种演进模式。

## 2026-08-09 · v1.9.0 — 世界备注与变更历史

**发布**：`v1.9.0` — 世界备注 + 变更历史 + 懒刷新

- `f099139` feat: world user notes + change history + lazy refresh（40 工具）
- `74` fix: parse worldId from location for user-location events

**演进意义**：世界元数据管理开始——为后续"世界推荐"打数据基础。

## 2026-08-09 · v1.10.0 — 周报与赞助

**发布**：`v1.10.0` — 周报功能 + 世界备注 + 赞助支持

- `66e7d5f` feat: get_weekly_report — VRChat 周报（41 工具）
- `4f5b82a` docs: 赞助区（微信+支付宝收款码）

**演进意义**：项目开始有"对外"属性（赞助）；周报是第一个面向用户的定期报告工具。

## 2026-08-09 · v1.11.0 — 新世界追踪 + 存储引擎重构

**发布**：`v1.11.0` — 新地图追踪器 + 搜索工具 + 存储引擎重构（PR #1 @uhu005）

**关键改动**：
- `70e4415` refactor: **sql.js → better-sqlite3**（WAL 崩溃安全）——根因级存储引擎重构
- `ad961db` feat: new world tracker - 自动收集新世界到 VRCX 本地收藏（PR #1，首个外部贡献者 PR）
- `f69ca18` feat: search_groups / search_worlds
- `66e7d5f`-`65` docs: DEVELOPMENT.md 贡献者指南 + AI-first 定位重写（README 44 工具）

**演进意义**：双里程碑——(1) 首个外部 PR（uhu005）验证了 fork/PR 流程可行；(2) 存储引擎换 better-sqlite3 解决 SQLITE_CORRUPT，确立"DB 变更带迁移"的开发约束。

## 2026-08-09 · v1.12.0 — 新世界追踪接入 MCP

**发布**：`v1.12.0` — 新世界追踪接入 MCP（scan_new_worlds / get_new_worlds）

- `58` feat: 新世界追踪器集成进 MCP 工具（46 工具）
- `64` feat: WS fallback proxy 可用 VRC_MONITOR_WS_PROXY 环境变量覆盖

**演进意义**：新世界追踪从独立脚本（VRCX 收藏路径）演化为标准 MCP 工具；代理覆盖支持解决部分网络环境 WS 连不上问题。

## 2026-08-10 · v1.13.0 — open-world 探测式本机增强

**发布**：`v1.13.0` — open-world 探测式本机增强 + create_instance / invite_myself（PR #5 @uhu005）

- `ff24fed` feat: open-world 命名管道直发 + API 邀请静默回退（PR #5）
- `3eabc4d` feat: create_instance / invite_myself（46→48）
- `52cc889`-`53` docs: 开发约束允许探测式本机增强（命名管道 IPC，静默回退）

**演进意义**：确立"探测式本机增强 + 静默回退"架构模式（写入 DEVELOPMENT §3.1），是后来跨平台约束的核心范式；讨论见 issue #4。

## 2026-08-10 · v1.14.0 — 好友推荐工具集

**发布**：`v1.14.0` — 好友推荐工具集 + 一键打开世界（PR #6 @uhu005）

- `4519558` feat: 好友推荐/偏好/学习 MCP 工具集（recommend_join + 评分体系，49→55）
- `f05acf3` feat: invite_myself 统一为 openInstance 引擎（与 open_world 同源）
- `46` docs: 仓库更名 vrchat-assistant

**演进意义**：推荐系统起步（recommend_join）；**仓库正式更名 vrchat-assistant**（原 vrc-monitor）；邀请逻辑收敛为单一引擎。

## 2026-08-10 · issue #3 / #7 — 署名修正

- #3: release v1.11.0 贡献者署名写错（uhu005 应为 shine trick）
- #7: Releases 感谢中 @shine trick 因空格截断成 @shine

**演进意义**：首个"社区元问题"——贡献者署名的正确性。shine trick = GitHub uhu005。

## 2026-08-11 · v1.15.0 — 架构重构

**发布**：`v1.15.0` — 架构重构：薄入口 + 13 模块（PR #9 @ggg123124）

- `3f1bb1c` refactor: 拆分 start-monitor.js 2735 行为薄入口 + 13 个模块
- `38` chore: 移除 archive/（含硬编码 auth token 的死代码）
- `39` docs: 修复跨文档不一致

**演进意义**：单体 2735 行拆分为 core/ 模块化架构（ws-manager / event-pipeline / storage / rpc-router / handlers/ 等），奠定后续可维护性；ARCHITECTURE.md 的雏形。

## 2026-08-11 · 学习系统扩展（PR #8 @uhu005）

- `d33788f` feat: 学习系统 5 维度扩展 — 重复选人/人数舒适区/类型偏好 + 评分权重均衡

**演进意义**：推荐系统从"静态评分"走向"用户行为学习"。

## 2026-08-12 · v1.16.0 — 迁移脚本健壮性三连修

**发布**：`v1.16.0` — 迁移脚本健壮性三连修（PR #10/#12/#14 @sansuiUwU）

- `7019b9b` fix: 迁移脚本增加服务运行检测，防止运行中迁移导致 SQLITE_CORRUPT（PR #10）
- `b017945` fix: 迁移脚本改用 better-sqlite3，根因修复 SQLITE_CORRUPT（PR #12 / issue #11）
- `eedde79` fix: 迁移幂等防重，重复执行不再重复插入 events（PR #14 / issue #13）

**演进意义**：外部贡献者（sansuiUwU）对迁移链路的系统性加固——运行检测 → 引擎统一 → 幂等防重，三层问题逐层击破；文档同步义务（DEVELOPMENT §1 漏洞上报）在此版本强化。

## 2026-08-12 · 社区建设

- `29-28` docs: README 增加 QQ 交流群（851865556）
- `31` docs: 重写 README 功能部分为能力域分组（对齐 55 工具能力地图）
- `24-26` docs: Agent 漏洞上报义务强化（查重、根因定位、fork 不豁免）

**演进意义**：README 从平铺列表升级为"能力域分组"，社区渠道（QQ 群）开通。

## 2026-08-13 · v1.17.0 — 世界推荐/收藏闭环

**发布**：`v1.17.0` — 世界推荐/收藏闭环（PR #16/#17/#22/#23/#24）

**关键改动**：
- `21` feat: 群组热度 get_group_heat + gmem_ 群组 ID 兼容（PR #16 @sansuiUwU）
- `20` feat: PlanetVRC 地图检索推荐 search_planet_worlds / recommend_planet_worlds（PR #17 @sansuiUwU）
- `19` fix: search_users 无匹配返回不相关用户 — API 子串退化匹配未过滤（PR #22 @Sand-85 / issue #20）
- `18` fix: decodeEntities 双重解码（CodeQL js/double-escaping）
- `17` feat: search_users API 无匹配回退本地好友库模糊搜索（PR #23 @Sand-85）
- `15` feat: 开图闭环用户反馈 — rate_world / mark_world_visited / user_rating 评分加权 / excludeTheme（PR #24 @sansuiUwU / issue #19）
- `9` feat: recommend_worlds 多源融合世界推荐（issue #18）
- `8` feat: favorite_world 云端收藏工具（issue #25）
- `11-14` fix: Review 修复（命名参数、worldScore 接线、tags 空串崩溃）

**演进意义**：**世界推荐闭环成型**——检索（PlanetVRC）+ 推荐（多源融合）+ 反馈（评分/收藏/排除）三段打通，是项目当前最完整的业务闭环；search_users 的"无匹配退化"问题由 PR 审查链（#21→#22→#23）逐步解决，体现 review 流程价值。

## 2026-08-13 · BOOTH 素材检索（PR #26/#27/#29 @Sand-85）

- `8837e48` docs(skill): 地图展示格式规范 — 序号/封面/热度/备注后地图链接（PR #26）
- `1e909be` feat: BOOTH 素材检索工具 search_booth_items / get_booth_item + 展示 skill（PR #27 / issue #27）
- `edd333d` feat: BOOTH 检索结果落库 — booth_items 快照缓存 + 搜索历史 + get_booth_history/get_booth_searches（PR #29 / issue #28）

**演进意义**：BOOTH 检索从"无持久化"到"快照缓存 + 搜索历史"闭环（issue #28 驱动的增量演进）；工具数 62→66（PR #27 加 2，PR #29 加 2）；展示 skill（地图展示格式）固化为用户验收过的格式规范。

## 2026-08-14 · v2.0.0 — 世界资料库平台化：数据本地化 + 生成物治理 + 三域工具集

**发布**：`v2.0.0` — 架构大版本（PR #26/#27/#29/#30/#31/#32/#33）

**关键改动**：
- `33` feat: 收藏世界分类分析 get_my_favorite_worlds / get_my_favorite_groups + favorites-pdf.py 一键中文 PDF 手册（PR #33 @268500tao）
- `32` feat: X 博主世界推荐聚合 x_world_digest / x_scan_creators / x_creators / x_add_creator / x_remove_creator / x_worlds（PR #32 @268500tao）
- `30` feat: 資料路徑可配置 + Windows 常駐服務（開機自啟 / 崩潰自癒 / 每日修復報告，PR #30 @Heimo0520）
- `29` feat: BOOTH 检索结果落库 — booth_items 快照缓存 + 搜索历史 + get_booth_history/get_booth_searches（PR #29 @Sand-85 / issue #28）
- `27` feat: BOOTH 素材检索工具 search_booth_items / get_booth_item + 展示 skill（PR #27 @Sand-85）
- `26` docs(skill): 地图展示格式规范（PR #26 @Sand-85）；`31` docs(skill): QQ Bot 封面图按需询问（PR #31 @Sand-85）
- `14` fix: world_cache tags 双重序列化根因修复（upsertWorld 传数组 + 读防御 + 存量清理）+ PDF 页脚 file:// 修复（维护）
- `13` fix: booth search-card HTML 标签注入绕过（CodeQL #14，安全扫描已接入开发流，维护）
- `12` refactor: 中文简介翻译从仓库 JSON 迁移至本地 DB（world_zh_translations 表）+ 85 条翻译补全（维护）
- `cfbeae6` docs(skill): BOOTH 缓存交互规则改为用户拍板版（明确要新→实时/默认缓存/拿不准才询问）

**架构级变化（2.0 大版本依据）**：
1. **数据本地化**：个人数据（中文翻译）从仓库 JSON 迁至本地 DB——仓库不再分发个人化数据，每人一份自己的资料库
2. **生成物治理**：PDF 产物（pdf/）+ 图片缓存（img_cache/）全部 git 忽略——仓库只含代码，-523 行个人数据净移除
3. **三域工具集**：收藏（2）+ X 博主（6）+ BOOTH（4）——工具数 62→74，从"好友监控"扩展为"世界资料库平台"
4. **运维架构**：Windows 常駐服務（開機自啟/崩潰自癒/每日修復報告）+ 資料路徑可配置
5. **工程质量**：CodeQL 安全扫描接入（HTML 注入 bypass 即时修复）、演进记录制度化、AI Agent 身份表达规则

**演进意义**：**"世界资料库平台"成型**——从 v1.x 的"好友监控 + 工具拼装"升级为「检索（BOOTH/X/收藏）+ 推荐 + 反馈 + 个人资料库（PDF 手册）」的完整产品架构；数据与产物治理确立仓库边界（只含代码）。

## 2026-08-14 · 文档治理三连：演进记录制度化 + 去工具总数 + AI Agent 身份表达规则

- `145e12e` docs: README 引用演进记录 docs/history/（新 Agent 上手引导）
- `ddf13e7` docs: 全仓库移除 MCP 工具总数表述（避免随工具膨胀反复维护），INDEX 维护规则同步
- `acb6732` docs(dev): 新增身份表达规则 — issue/PR/commit 一律以 AI Agent 口吻提交（§1 原则 + §2 第13条硬性要求）

**演进意义**：三个 docs commit 确立项目文档治理模式——(1) 演进记录成为新 Agent 的必读入口（README 直接引用 docs/history/，解决"新 Agent 不了解项目来龙去脉"的问题）；(2) 全仓库移除"N 个 MCP 工具"数字表述：工具持续膨胀下数字维护成本高、极易漂移，改为只维护工具名清单（有就是有，没有就是没有）；(3) 身份表达规则：本项目除作者外贡献者均为 AI Agent，issue/PR/commit 一律以 AI Agent 口吻提交，避免不同 Agent 口中的"用户"被混为同一人。

## 2026-08-14 · 新 PR：资料路径可配置 + Windows 常驻服务（PR #30 @Heimo0520，已合并）

- `#30` (MERGED, 2026-08-14) feat: 资料路径可配置 + Windows 常驻服务 — 开机自启 / 崩溃自愈 / 每日修复报告
  - `VRC_MONITOR_DB_PATH` / `VRC_MONITOR_BACKUP_DIR` 数据路径可配置（未设置则向后兼容）
  - `service-windows/`：launcher（detached 幂等启动）+ watchdog（每分钟健康检查、崩溃自动重启、静默）+ 每日 09:00 修复报告（无修复不通知）+ setup-windows.cmd 一键注册
  - 作者已验证：真实崩溃修复 / 幂等 / 日报 / 中文系统兼容 / 数据迁移

**演进意义**：项目第一次收到"常驻服务化"提案（来自 Heimo0520 背后用户的运维场景）——把 vrc-monitor 从"手动启动的进程"升级为"系统服务"，确立守护进程 + 静默运行 + 数据路径可配置的运维模式；已合并（2026-08-14），新增 `service-windows/` 目录与一批环境变量约定。

## 2026-08-14 · fix: recommend_worlds 场景化参数生效（theme 软过滤 + excludeTheme 官方标签复查，#34）

- `26d56a5` fix(world): recommend_worlds theme 软过滤生效 + excludeTheme 补官方标签复查（作者直推 main，issue #34 实测报告驱动）
  - `theme` 软过滤：theme≠default 时只保留主题命中候选（sleep_ok 强信号/关键词命中），命中为空回退全量防空结果；返回值新增 `themeFiltered` 字段供 Agent 判断
  - `excludeTheme` 增强：候选阶段 world_cache tags 不完整导致 Lucky Paw（官方 tags 含 game）漏网 → isExcludedByTheme 用 world_cache 补全官方 tags 复查，精确/子串命中即剔除
  - 实测：excludeTheme='game' 剔除 Lucky Paw ✅；theme=sleep 只留 4 个睡觉图 ✅；theme=chat 只留咖啡厅 ✅；默认调用回归正常 ✅

**演进意义**：v2.0.0 发布后第一个针对性修复——此前 recommend_worlds 的场景化参数形同虚设（不同 theme 返回完全相同候选、主题加分从未触发、excludeTheme 漏网官方标签），说明"多源融合推荐"上线时参数链路未真正打通；本次确立**软过滤模式**（强信号优先 + 空结果回退：宁可回退全量也不返回空）+ **官方 tags 复查机制**（候选阶段 tags 不完整是漏网根因），让场景化推荐从"参数存在"走向"参数生效"，并为 Agent 暴露 `themeFiltered` 透明化字段。

## 2026-08-15 · 发布 v2.0.1（文档治理 + 开发 skill + 检测增强）

**发布**：`v2.0.1` — 小版本（无新 MCP 工具，文档/skill 治理 + fix #34 已在 v2.0.0 后单独记录）

**关键改动**：
- `docs` README 重构：人类优先精简版（428→83 行）+ 新增英文/日文版 README.en.md / README.ja.md + 首页语言切换链接；工具权威登记位置从 README 迁移至 Agent Skill（避免重复维护）
- `feat(skills)` 新增 vrchat-assistant-development 开发 skill：流程编排 + 仓库导航（Phase 0 Fork 归属检查 / 强制读 DEVELOPMENT.md / 三件套位置），规范正文以仓库文件为准防上游漂移；AGENTS.md 安装章节纳入 4 份 skill
- `feat(scripts)` check-doc-drift 检测增强：历史记录 PR 状态漂移检测（OPEN→MERGED 漏改）+ skills/ 目录一致性（工具引用死链 / README skill 提及不一致）+ 扫描覆盖多语言 README
- `docs(agents)` X 博主世界推荐追踪：推荐博主清单（ヤタノ/もっけい/八谷凛奈/Bradlee101）+ 安装阶段主动询问用户确认后添加
- `fix` recommend_worlds 场景化参数生效（theme 软过滤 + excludeTheme 官方 tags 复查，PR #34，见 8-15 上方条目）
- 历史记录修正：PR #30 状态标注从 OPEN 更新为 MERGED（演进意义措辞按现状）

**演进意义**：v2.0.0 架构大版本后的**文档治理版本**——确立「README 面向人类、工具清单面向 Agent、开发规范以仓库文件为唯一权威」的三层文档分工；开发 skill 让任何 Agent 无需重复阅读即可按规范开发；检测脚本从「工具登记完整性」扩展到「文档与现状全链路一致」（PR 状态 / skill 死链 / 目录一致性），把文档漂移的检查面补齐。

## 2026-08-16 · 新 PR：待逛地图列表（world backlog）+ 表名重构 new_worlds → world_kb（#35）

- `#35` (MERGED, 2026-08-16) feat(world-backlog): 待逛地图列表 `add_to_backlog` / `get_backlog` / `remove_from_backlog` @nixi-agent
  - 痛点：用户检索/推荐发现好图后不会马上去逛，需要「待逛列表」先存着（工作流：发现好图 → add_to_backlog → 之后 get_backlog → 拍板 → open_world）
  - **合表方案**：不建新表，`new_worlds` 上加 `backlog` / `backlog_added_at` / `backlog_reason` / `backlog_priority` 4 列——该表已演化为「本地世界知识库 + 用户状态表」（user_rating/visited/sleep_ok 均在其上），待逛本质是又一个用户状态
  - 三联动零成本：逛完自动清出（user-location 事件回写 visited 时顺手 backlog=0）/ 推荐不重复推（recommend_worlds、get_new_worlds(onlyUnvisited) 排除待逛中）/ 详情零 JOIN（get_backlog 一次查询全拿到）
  - 幂等 PRAGMA 迁移；重复 add 更新备注/优先级且 addedAt 保持首次；priority/limit 钳制；纯本地功能不触碰 VRChat 云端（区别于 favorite_world 云端收藏）
  - 附带 commit：`new_worlds` 表更名 `world_kb`（表早已名不副实：scan 追踪 / user_rating / visited / sleep_ok / backlog 全住在里面；更名与 `world_cache` 详情缓存语义区分；**MCP 工具名 `scan_new_worlds` / `get_new_worlds` 为 API 契约保留原名不动**）+ SKILL.md 工具表登记三工具
  - 作者已验证：迁移三场景（新库直建 / 老库 RENAME 数据保留 / 真实库副本 197 行无损）、真实库 MCP 实测闭环、33.8 万 events 无丢失

**演进意义**：补齐「发现 → 待逛 → 开图 → 逛完清出」的本地闭环——此前开图闭环只有「收藏/烂图/visited」，缺「暂不逛但想记着」的中间态，用户只能靠外部笔记；本 PR 确立**合表方案模式**（用户状态一律并入 world_kb 单表，靠事件驱动联动而非新表 + 手动维护），并以表名重构固化「表随职责演化」的认知——名字承载语义，名不副实就该改，但对外 API 契约（工具名）保持稳定。

## 2026-08-16 · 发布 v2.1.0（在线好友信息增强：世界名 + 停留时长 + 在线时长）

**发布**：`v2.1.0` — 小版本（get_online_friends 信息增强 + PR #36 审核收尾，无新增工具）

**关键改动**：
- `feat(online-friends)` get_online_friends 三连增强：
  - 世界名 `worldName`：未缓存时批量 API 查询并写 world_cache（不再留 null）
  - 停留时长 `durationMinutes` / `enteredAt`：进入事件 → 停留统计
  - 在线时长 `onlineMinutes` / `onlineSince`：修复停留时长跨会话污染（session 边界判定）
- `fix(online-friends)` 采纳 PR #36 审核建议 W1/W2/W4（friends.js + storage.js 收尾）
- `docs(skill)` vrc-monitor-agent 工具表登记新字段

**演进意义**：在线好友列表从「人在哪」升级为「待多久」——世界名不再 null（批量回填 world_cache 补齐详情缓存）、时长字段让 Agent 能判断「好友刚来还是快走了」（跨会话污染修复保证统计准确）。在线列表成为五要素固定输出：昵称 | 世界 | 房型 | 在线时长 | 房间停留。

## 2026-08-16 · 发布 v2.1.1（公共 Skill 按对象域拆分 + 文档治理）

**发布**：`v2.1.1` — 纯文档/规范更新（无代码改动、无新 MCP 工具、无行为变化）

**关键改动**：
- `docs` 公共 skill 按对象域拆分（对应本地 Agent skill 结构）：
  - `vrc-monitor-agent` 收敛为总纲——全部 MCP 工具清单（唯一权威）+ 通用陷阱 + 服务健康检查 + 子 skill 路由索引
  - 新增 `vrchat-social-queries`：好友/社交域工作流（在线五要素/同屏/上线规律/画像/关系 + 全部社交写操作）
  - 新增 `vrchat-world-queries`：世界域工作流（挑新世界/backlog/推荐/PlanetVRC/情报挖掘/X 博主）
  - 新增 `vrchat-group-queries`：群组域工作流（群组查询/公告 403 分诊/join/leave/peek）
  - 删除 `vrc-monitor-companion-query`（内容并入社交域）
- `docs` 去敏感化：公共 skill 全量扫描——真实 userId / 群组 ID / 邮箱 / 本机路径 / 个人昵称 0 命中，全部替换为占位符或通用表述
- `docs(agents)` 安装说明同步新 skill 列表；AGENTS.md skill 表格同步

**演进意义**：公共 skill 从「单一巨型总纲」演进为「总纲 + 按对象域拆分」——总纲保持工具清单唯一权威（单一事实源不随拆分稀释），域 skill 专注各自工作流与陷阱，与本地 Agent skill 结构对齐，Agent 按能力域路由加载；去敏感化确立公共仓库安全分发基线：skill 不允许出现真实个人数据（userId/群组 ID/邮箱/路径/昵称），全部占位符化。

## 2026-08-17 · fix: MCP 服务端响应 ping 方法（长连接客户端 keepalive 挂起，issue #37）

- `f511e23` fix: MCP 服务端响应 ping 方法，修复客户端 keepalive 挂起超时（作者直推 main，issue #37 实测报告驱动，2026-08-17 01:19）
  - 现象：Hermes（MCP Python SDK，Streamable HTTP 传输）调用工具偶发挂起至 300s tool_timeout（实测 `get_weekly_report` 300.02s 超时）；**同刻 curl 直连 `:8799/mcp` 完全正常**（13.2s 返回）——排查迷惑点
  - 根因：`core/rpc-router.js` `handleRpc()` switch 只有 initialize / notifications/initialized / tools/list / tools/call 四个 case，`ping` 落 default → 返回空 SSE 响应（HTTP 200 + Content-Length: 0）；MCP Streamable HTTP 规范要求 ping 必须返回 `{"jsonrpc":"2.0","id":<原id>,"result":{}}`
  - 影响链：客户端 keepalive 每 ~3-4 分钟 ping（30s 超时）→ 等不到带匹配 id 的响应 → 判死重连 → 5 次连续失败进入 parked（300s 自检）→ parked 窗口内工具调用全部挂起；所有长连接客户端（官方 SDK / Hermes / Claude Desktop）受影响，一次性脚本/裸 curl 不受影响（新建连接直接 POST tools/call）
  - 修复：rpc-router.js 补 ping case 按协议返回 JSON-RPC 响应 + `core/http-server.js` 补 `DELETE /mcp` 路由（此前 SDK 终止会话 404：`Session termination failed: 404`）

**演进意义**：第一次暴露 MCP 协议合规性缺口——服务端此前只实现了业务方法（initialize/tools/*），协议级方法 ping 缺失让标准长连接客户端周期性判死重连、最终 parked 挂起至超时；确立两个模式：(1) 协议方法必须按规范返回带原 id 的 JSON-RPC result，空响应 = 客户端永远等不到结果；(2)「脚本/curl 直连正常 ≠ 长连接客户端正常」——排查 MCP 服务问题必须用长连接客户端复现，一次性连接会漏掉 keepalive / 会话生命周期类缺陷。

## 2026-08-17 · fix: OTP 自动登录支持 VRC_MONITOR_PYTHON 指定解释器路径（PR #38）

- `#38` (MERGED, 2026-08-17) fix: OTP 自动登录支持 VRC_MONITOR_PYTHON 指定解释器路径（修复重试循环刷验证码邮件）@Heimo0520
  - 痛点：`core/otp-fetcher.js` 硬编码裸 `python` 执行 fetch-otp.py；计划任务 / systemd / 容器等 PATH 无 python 的环境下 spawn 失败 → OTP 自动登录循环重试，**每次循环 VRChat 都会重新发送验证码邮件**（实测邮箱收到大量验证码）——静默失败 + 放大效应的典型案例
  - 修复：改用 `VRC_MONITOR_PYTHON` 环境变量指定解释器路径（默认仍为 `python`，向后兼容），AGENTS.md 环境变量清单同步登记
- `7cc9aeb` fix(otp-fetcher): 含空格路径自动加引号（Windows cmd 首 token 截断）——`${pythonBin}` → `"${pythonBin}"`，AGENTS.md 注明「路径含空格无需自带引号」（如 `C:\Program Files\Python311\python.exe`）

**演进意义**：修复链展示「环境可配置化 + 平台引号语义」双层坑——第一个 commit 解决 PATH 无 python 的部署环境（环境变量 + 默认值向后兼容），两分钟后第二个 commit 立刻踩中 Windows cmd 首 token 截断（路径含空格时裸拼命令被空格切断），确立模式：**解释器/路径类配置一律用环境变量，命令拼装对可能含空格的 token 统一加引号**；同时「失败循环每次触发 VRChat 重发验证码邮件」提示 OTP 类自动登录的重试必须有退避，否则故障会污染邮箱、放大表象。

## 2026-08-17 · docs: 署名规则同步——署名可用使用者账号，文字必须 Agent 口吻

- `117b6a6` docs: 同步署名规则（DEVELOPMENT.md §1「身份表达」）：**署名与口吻分离**——Agent 提交者署名（commit author）可以使用背后使用者的 GitHub 账号 / 邮箱（Agent 使用使用者账号提交是允许的），但所有文字必须以 Agent 自身口吻书写，不得以人类第一人称发言；§2 PR 要求第 13 条同步

**演进意义**：把「Agent 提交时身份表达」从「署名必须是 Agent 自己的身份、不得冒用使用者账号」修正为「署名可用使用者账号、文字必须 Agent 口吻」——实操中 Agent 常被配置为使用使用者的 git 身份提交（本仓库即如此），旧规则与现状冲突导致每次提交都要权衡；新规则确立可操作的边界：署名归使用者（账号层）、口吻归 Agent（内容层），审查方仍能一眼区分「Agent 提交的内容」与「使用者本人的需求」。

## 2026-08-18 · 发布 v2.2.0（协作审核协议 + OTP/连接健壮性加固）

**发布**：`v2.2.0` — 小版本（无新增 MCP 工具）：协作审核协议体系 + 跨平台迁移探测 + MCP 协议合规与 OTP 修复链

**关键改动**：
- `feat(agent-review)` 新增协作审核协议与参考脚本（AGENT-REVIEW.md + scripts/agent-review.py，640 行）：审核视角条款 §3（按自身环境与使用者习惯个性化 review）、重要关联豁免（与自身/使用者有重要关联的 PR/issue 不受满员限制）
- `feat(migrate)` 迁移/分析脚本支持跨平台自动探测 VRCX 数据库路径（PR #40）
- `fix` MCP 服务端响应 ping 方法，修复客户端 keepalive 挂起超时（issue #37，协议合规：ping 必须返回带原 id 的 JSON-RPC result）
- `fix(otp)` OTP 自动登录支持 VRC_MONITOR_PYTHON 指定解释器路径（PR #38）+ 含空格路径自动加引号（Windows cmd 首 token 截断，7cc9aeb）
- `fix(otp)` 归一化 IMAP SEARCH 空结果形态（163 '(none)' / Gmail b'' / 失败态），移除重复赋值（PR #41）
- `docs` 署名规则同步（Agent 可用使用者账号提交，文字必须 Agent 口吻）+ 演进记录维护 ×2

**演进意义**：v2.2.0 确立协作审核协议（AGENT-REVIEW）作为外部贡献接入规范——审核视角个性化让参与审核的 Agent 按自身环境执行、重要关联豁免避免自家人审自家事卡满员；OTP 修复链固化「解释器路径一律环境变量 + 空格 token 加引号」工程模式；MCP ping 修复补上协议合规基线（协议方法必须返回带原 id 的 JSON-RPC result，空响应 = 长连接客户端永远等不到）。
## 2026-08-18 · feat(totp): TOTP 两步验证登录，新增 submit_totp MCP 工具（PR #45 @CcendercatcC，已合并）

- `#45` (MERGED, 2026-08-18) feat(totp): 支持 TOTP 两步验证登录，新增 submit_totp MCP 工具
  - 需求：使用者账号启用了 VRChat TOTP（Authenticator 应用）两步验证，现有流程只支持邮箱 OTP 自动抓取，无法完成登录
  - `vrchat-api.js` 按 `requiresTwoFactorAuth` 数组分流 emailOtp/totp，提取统一 `_verify2fa(code, type)`；邮箱 OTP 抓取失败时若账号同时支持 TOTP，兜底转手动提交（保留 tempAuthCookie）
  - 新增 `submit_totp` MCP 工具（三件套：mcp-definitions 定义 + handlers/auth.js + rpc-router 路由），校验 6 位数字；`/health` 暴露 `auth.needsTotp`；登录成功后自动清状态、重置 auth 冷却、触发 WS 重连
  - 实测：needsTotp 状态 → submit_totp 提交验证码 → 登录成功；验证码错误/非 6 位被拒；邮箱 OTP 路径回归通过

**演进意义**：认证从「只有邮箱 OTP 一条路」扩展为「邮箱 OTP + TOTP 双通道」——TOTP 账号（更安全的 Authenticator 应用）此前直接无法登录，是登录面的真实缺口；确立 **2FA 分流模式**：按 `requiresTwoFactorAuth` 数组区分验证类型、统一 `_verify2fa` 包装、邮箱 OTP 失败自动兜底 TOTP 手动提交，登录路径从此对账号安全等级透明。

## 2026-08-18 · docs(skill): 世界推荐新增 chill 向过滤规则（PR #44 @Sand-85，已合并）

- `#44` (MERGED, 2026-08-18) docs(skill): 世界推荐新增 chill/放松向过滤规则（vrchat-world-queries skill §6，真实使用中用户拍板 2026-08-15）
  - 排除随机聊天/轮盘配对社交向（如 No Time Two Talk、omegle 式 roulette）——主题 social/chat 的不算 chill
  - 聚焦 sleep/onsen/温泉/居家/小憩系（author_tag 含 chill/sleep/relax/spa/onsen）
  - 备注列标注当前人数（occupants/capacity）——爆满的图即使主题 chill 也提示人多；候选补热度（recommend_worlds 输出 heat 常为 0，用 get_world_name 逐个补查 favorites）

**演进意义**：「chill 的室内图」是高频自然语言需求，但此前推荐会混入随机聊天配对图（被用户指出不属于 chill）——确立 **场景语义规则化模式**：把口头偏好固化为 skill 中的可执行规则（排除项 + 聚焦项 + 人数提示），让所有使用者的 Agent 推荐行为一致，而不是每次现问现答。

## 2026-08-18 · fix(migrate): 补迁 gamelog_location 自己的位置历史 + 显式 userId 迁移 0 条修复（PR #47，已合并 + 直推 commit）

- `#47` (MERGED, 2026-08-18) fix(migrate): 迁移 VRCX gamelog_location 补全自己的位置历史——此前 migrate-vrcx0.mjs 只迁好友侧 feed_* 系列，漏掉记录本账号自己位置的 gamelog_location 表 → storage.findCompanions 依赖的 user-location 事件在迁移前窗口无数据 → `get_companions` 查自己的历史同屏全为 0（issue #46）
  - 新增迁移段（feed_gps 之后编号 5.5）：gamelog_location → type=user-location、user_id=自己，content_json 含 location/worldName/time/groupName + vrcxId(selfloc:)
  - 新增 `userIdFromPrefix()`：VRCX 表名前缀（usr+32 位 hex 无横线）转标准 `usr_xxx-xxx` 格式；沿用 insertInBatches 分批事务 + vrcxId 唯一索引，幂等（重复执行自动跳过）
  - 实测：真实库迁移 1844 条，复跑不重复；get_companions 近 7 天窗口恢复 userInstanceCount: 104
- `1f10552` fix(migrate): 修复命令行显式传 userId 时迁移 0 条（resolve_user_prefix 未去下划线）

**演进意义**：迁移漏表暴露「只迁好友侧、漏自己侧」的对称性缺口——同屏分析（get_companions）的数据基础是 user-location 事件，自己没有位置历史则「同屏」无从谈起；确立迁移完整性检查法：**分析型能力必须核对依赖事件在迁移前窗口是否有数据**（查不到历史 = 先怀疑迁移覆盖不全，而非查询逻辑错）；幂等 vrcxId 方案再次复用，证明「唯一索引 + INSERT OR IGNORE」是迁移段的通用模板。

## 2026-08-18 · docs: 新增 pr-review 公共 skill（PR 审核流程方法论，直推 main）

- `7a006ba` docs: 新增 pr-review 公共 skill（PR 审核流程方法论）
- `5e7f102` docs: review-workflow 重命名并扩展覆盖 issue 审核

**演进意义**：审核流程从「项目内部约定」升级为「公共 skill」——pr-review 把 PR 审核方法论独立成可复用资产（与仓库内 AGENT-REVIEW.md 协议配套），review-workflow 重命名后覆盖范围从 PR 扩展到 issue 审核，形成「协议（AGENT-REVIEW §3 个性化视角 + 重要关联豁免）+ 操作手册（skill）」双层结构。

## 2026-08-18 · fix(monitor): 8799 端口预检前置——双实例 OTP 互抢直接退出（PR #50 @nixi-agent，已合并）

- `#50` (MERGED, 2026-08-18) fix(monitor): 8799 端口被占用时仅警告不退出，双实例并存引发 OTP 验证码互抢循环（issue #49）
  - 事故链：core/http-server.js EADDRINUSE 只 log 不 exit → 双实例并存 → 两实例同时进认证互抢 OTP 验证码 → VRChat 重复下发 → 邮箱验证码轰炸（实测持续 2.7h）
  - 主修复：main() 开头新增 `isPortBusy()` 预检（复用 migrate 脚本的 net.connect 探测），被占即输出排查命令（Windows netstat / Linux ss）并 process.exit(1)——**预检位于凭据加载/认证/OTP 之前，退出也不浪费验证码**
  - 兜底：http-server 对 EADDRINUSE 改警告 + process.exit(1)，双保险；AGENTS.md 常见问题补「端口 8799 被占用」条目
  - 验证：端口被占端到端实测第二实例预检即退出无副作用；空闲端口放行不误杀正常启动

**演进意义**：「端口被占仅警告」是静默失败变灾难的教科书案例——警告不退出让第二实例带着完整认证流程跑起来，两个实例互抢验证码造成邮箱轰炸；确立 **单实例守护模式**：端口预检前置到一切副作用（凭据/认证/OTP）之前 + 监听失败兜底退出，双保险杜绝「看似启动成功实则已在运行」；预检放行空闲端口保证模式不误伤正常启动。

## 2026-08-18 · fix(auth): 运行期 API 401 自动重登录，TOTP 账号免重启恢复（PR #52 @CcendercatcC，已合并）

- `#52` (MERGED, 2026-08-18) fix(auth): 运行期 API 401 自动重登录，TOTP 账号免重启恢复（issue #51）
  - 需求：服务运行中 VRChat cookie 过期后所有 API 工具 401 失效，无自动重认证，必须重启服务；TOTP 账号重启后还要重新提交验证码
  - `vrchat-api.js` 新增 `_requestRaw` + 带 401 拦截的 `_request`：401 时调用 `_tryAutoReauth`（复用 ensureAuthWithAutoOtp，邮箱 OTP 优先、TOTP 兜底），single-flight 防并发重认证，非 TOTP 失败冷却 60s 防循环，`_isAuthEndpoint` 排除 /auth* 防内部递归
  - 需要 TOTP 时抛 needsTotp 并保留 tempAuthCookie，`submit_totp` 可直接完成登录（与 PR #45 闭环）；rpc-router catch 层同步 serverState.needsTotp，`/health` 感知待验证状态
  - 实测：篡改内存 cookie 模拟失效 → 401 自动重登录 → 新 cookie 请求 200 正常；全程无需重启服务

**演进意义**：把「401 = 会话失效」从终态（必须重启）变为可恢复状态（自动重认证）——之前 cookie 过期是服务致命伤：所有工具失效 + 重启后 TOTP 账号还要人工再验证；确立 **会话自愈模式**：请求层统一拦截 401 + single-flight 防并发重认证 + 冷却防循环 + TOTP 待验证状态透出（/health 与 submit_totp 无缝衔接），补全了 #45 只解决登录、没解决运行期失效的缺口。

## 2026-08-19 · feat(world): get_worlds_by_author 按作者列出全部世界 + get_world_name 补 authorId（PR #53 @CcendercatcC，已合并）

- `#53` (MERGED, 2026-08-19) feat(world): 新增 get_worlds_by_author 按作者列出全部世界，get_world_name 补返回 authorId
  - 需求：使用者想「把当前所在图作者的全部图加权重」，但现有工具缺按作者列出世界的基础能力；与使用者讨论后拆成两个基础查询工具（而非组合式批量标记工具），便于 Agent 灵活组合：get_world_name → 拿 authorId → get_worlds_by_author → 遍历 rate_world
  - `get_worlds_by_author`：输入 authorId 或 authorName（互斥，authorName 经 /users?search 精确匹配解析 userId），GET /worlds?userId=&n=100&offset= 分页拉全该作者全部世界，顺带写 world_cache（含 author_id）；handler 内逐请求 rateLimiter.execute（RPC case 层不嵌套防死锁，参照 scan_new_worlds）
  - `get_world_name` result 增加 authorId；修复 upsertWorld 未持久化 author_id（原 F58220 等缓存 author_id 为空的根因）
  - 实测：authorName=もくそん 正确列出 17 张图（含当前所在 F58220）；forceRefresh 后 author_id 正确落库

**演进意义**：世界查询补齐「按作者」维度——此前只有按名字/热度/主题找图，没有「这个作者还做过什么」的聚合视图（作者维度对 Agent 的加权重/追更工作流是前置能力）；确立 **基础工具拆分优于组合魔法** 模式：用户诉求是「给当前作者全部图加权」，实现拆成两个可复用查询工具而非一次性批量标记，Agent 自由组合；修复 upsertWorld 不写 author_id 揭示「缓存缺字段」是关系链断裂的隐蔽根因。

## 2026-08-20 · feat: Linux systemd 用户服务托管（service-linux/，PR #42 @Menaed，已合并）

- `#42` (MERGED, 2026-08-20) feat(service): 新增 Linux systemd 用户服务托管（service-linux/）
  - 与 Windows 常驻服务（service-windows/，PR #30）平台互补：覆盖无头服务器 / VPS / NAS 场景
  - `vrc-monitor.service` systemd 用户单元（systemctl --user）：Restart=always + RestartSec=5 崩溃自愈（等价 Windows watchdog 轮询）、日志走 journald（符合 DEVELOPMENT.md §3.8 stdout 约定）、`%h` 路径占位零个人环境硬编码
  - `setup-linux.sh` 一键安装/卸载：解析仓库路径 / node / python 生成真实单元并 `enable --now` + `loginctl enable-linger`（登出后继续运行）；PATH 无 `python` 有 `python3` 时自动注入 `VRC_MONITOR_PYTHON=python3`，规避 OTP 登录重试循环陷阱；ExecStart 路径含空格时自动对含空格 token 加引号（systemd 按空白拆分参数，nit 修复）——env 模式（node 不在 PATH，`/usr/bin/env node`）保持两个 token 仅对路径加引号，避免整体被当单个可执行文件路径（审核边界项修复）
  - 文档同步：AGENTS.md 常用操作表拆分 Windows/Linux 两行、插件平台限制说明补 Linux 方案；README 文档导航登记 `service-linux/`
  - 审核修正记录：nixi-agent + sansuiUwU 双 Agent 审核 REQUEST_CHANGES（阻断项=docs/history 合并冲突）；rebase 后复验 APPROVE，两条 nit（ExecStart 空格引号、VRC_MONITOR_DIR 表述）已修复

**演进意义**：平台托管矩阵补齐 Linux 侧——Windows 常驻服务（#30）确立 watchdog 模式后，Linux 用 systemd 用户服务实现等价能力（崩溃自愈/静默运行/日志可查），且天然规避 systemd 环境无 python 的 OTP 循环坑（自动注入 VRC_MONITOR_PYTHON，与 #38 的配置化约定衔接）；「Windows watchdog + Linux systemd」双方案让不同部署场景各取所需。

## 2026-08-19 · fix: favorite_world 请求体契约修正（favoriteId 传对象 ID，PR #55）

- `b73a22e` fix: favorite_world 的 favoriteId 传对象 ID 而非分组名/分组 ID（issue #54 实测报告驱动，PR #55）
  - 修复前请求体 `{ type: 'world', favoriteId: tag, tags: [tag], worldId }` —— `favoriteId` 传分组名（worldsN）被 400 拒绝（`favoriteId must be an ID`），传 fvgrp_ 分组 ID 会按对象 ID 校验失败（`world fvgrp_... not found`）
  - 修复后 `{ type: 'world', favoriteId: worldId, tags: [tag] }`——按官方 Add Favorite 文档：`favoriteId` 必须是被收藏对象本身的 ID（world 类型为 wrld_...），分组由 `tags` 数组指定，删除无效 `worldId` 字段
  - 实测：`favorite_world {worldId: wrld_80a0ffdc-..., tag: 'worlds1'}` → 修复前 `favorited: false` + 400；修复后 `favorited: true`，本地 world_cache.favorited 标记写入

**演进意义**：favorite_world 云端收藏工具上线后首次暴露的 API 契约错误——请求体把「收藏分组」（tags 语义）误当成 favoriteId，与官方 Add Favorite 文档不符；本次修正确立请求体契约边界：**favoriteId 恒为对象 ID、分组恒走 tags**，并在 favorites.js 头注释固化该契约（供后续维护者参考），收藏链路（推荐 → 收藏 → 反馈加权）从此可用。

## 2026-08-19 · feat(friend-profile): 好友资料变更追踪（PR #56 @CyberNekokoya，已合并）

- `#56` (MERGED, 2026-08-19) feat(friend-profile): 好友资料变更追踪（Avatar/Bio/状态/头像图标/代词）
  - 需求：friend-update 事件推送完整 user 对象（currentAvatarImageUrl/bio/statusDescription/userIcon/pronouns），但原 event-pipeline 只存了 displayName，数据被浪费
  - `core/event-pipeline.js` `_handleUpdate`：解析 user 对象与 friends 表快照 diff，变化写入 events（type 与 VRCX 迁移脚本 feed_avatar/feed_status/feed_bio 一致，历史数据打通）；无历史快照（首次采集）只初始化不误报
  - `core/storage.js`：friends 表幂等迁移新增 bio/user_icon/pronouns 列；upsertFriend 扩展；新增 getFriendProfileChanges/getFriendProfileChangeCount 查询
  - 新增 MCP 工具 `get_friend_profile_changes`：按 userId（省略=全部）查 Avatar/Bio/状态/图标/代词变更历史，types 过滤，limit/offset 分页
  - 实测：curl 返回 48331 条历史（含 VRCX 迁移的 10 个月数据，同 type 打通验证）

**演进意义**：事件流「数据被浪费」的典型——friend-update 推送的完整 user 对象只存了 displayName；确立 **事件数据完整性原则**：推送里有价值的字段全部落库（快照 diff 事件化 + type 与迁移历史对齐），分析型工具（资料变更查询）才能覆盖迁移前窗口；friends 表加列幂等迁移模式再次复用。

## 2026-08-19 · feat(notifications): 通知收件箱（PR #57 @CyberNekokoya，已合并）

- `#57` (MERGED, 2026-08-19) feat(notifications): 通知收件箱（读取/已读/隐藏/接受/拒绝好友请求）
  - 需求：notification/notification-v2 事件原本只在本地被动记录，缺少主动读取与操作能力
  - 新增 `core/handlers/notifications.js` 5 个 MCP 工具（基于旧 v1 通知系统 /auth/user/notifications）：
    - `get_notifications`：读取收件箱，limit/offset 分页、types 过滤、hidden 查看已隐藏；API 的 type 查询参数已废弃不生效（本地过滤）
    - `see_notification` / `hide_notification`：标记已读（PUT .../see）/ 隐藏清除（PUT .../hide，旧 v1 hide 即删除）
    - `accept_friend_request`：接受好友请求（**接受即直接加为好友**，confirm: true 护栏）
    - `decline_friend_request`：隐藏拒绝（旧 v1 无独立拒绝端点），confirm: true 护栏
  - 端点细节经社区维护的 OpenAPI 规范（vrchatapi/specification，vrchat.community/openapi.json 为其文档门户——VRChat 官方从不提供 API 文档，规范本身是社区逆向整理的产物）与 VRCX 源码交叉验证；群组邀请/活动属 NotificationV2（/notifications 端点）不在范围
  - 实测：get_notifications 返回 friendRequest 通知；accept_friend_request 真实接受好友申请，get_friend_info 验证 isFriend=true

**演进意义**：把「被动记录的通知事件」升级为「可操作的收件箱」——好友请求的接受/拒绝此前完全依赖 VRChat 客户端手动操作，Agent 无法介入；确立 **被动记录 → 主动操作闭环** 的升级路径：同一数据源补齐读写；接受/拒绝好友这类不可逆社交操作配 confirm 护栏（与写操作类工具同规则）。

## 2026-08-19 · feat(friend-favorites): 好友收藏分组管理（PR #58 @CyberNekokoya，已合并）

- `#58` (MERGED, 2026-08-19) feat(friend-favorites): 好友收藏分组管理（查询/添加/移除/移动）
  - 需求：原只有只读工具 get_favorite_friends_locations（看组内好友实时位置），补齐写操作闭环
  - 新增 `core/handlers/friend-favorites.js` 4 个 MCP 工具（favorites type=friend）：
    - `get_friend_favorite_groups`：GET /favorite/groups?type=friend + /favorites → 分组名/显示名/成员数
    - `favorite_friend`：POST /favorites {favoriteId: userId, tags: [name], type: friend}；须已是好友（403 not friends）；重复收藏已存在不抛错。confirm 护栏
    - `unfavorite_friend`：先 GET /favorites 找记录 id（fvrt_，非 userId）再 DELETE /favorites/{id}；groupName 可选（省略=全部分组移除）。confirm 护栏
    - `move_friend_group`：删旧建新（API 无原地更新 tags 端点，与 VRCX 行为一致）。confirm 护栏
  - ⚠️ 限流注意：case 层不包 rateLimiter——handler 内部（fetchFriendGroups）已逐请求限流，外层包裹会死锁（DEVELOPMENT.md §5 事故教训，开发中实测命中并已修正）
  - 实测：favorite → unfavorite 完整闭环（48→49→48）；move 到不存在分组正确报错

**演进意义**：只读工具补齐写闭环——好友收藏分组从「看得到」到「管得了」；**限流放置铁律**再次验证（handler 内部限流与 case 层包裹互斥，叠包死锁）；confirm 护栏覆盖全部写操作（favorite/unfavorite/move），不可逆社交操作（删分组、移分组）必须有护栏。

## 2026-08-19 · fix(auth): needsTotp 状态被 /health 掩盖（PR #60 @CcendercatcC，已合并）

- `#60` (MERGED, 2026-08-19) fix(auth): needsTotp 状态下 /health 与 get_server_status 仍报 authenticated:true，掩盖待验证状态（issue #59）
  - 问题：TOTP 账号运行期 401 自动重认证进入 needsTotp 待验证态后，/health 与 get_server_status 仍报 authenticated:true，Agent 只查这两处会误以为正常，实际 API 调用才抛「需要 TOTP 验证码」；与文档承诺（/health 的 auth.needsTotp: true 时调用 submit_totp）不符
  - 修复（issue 期望行为的备选方案）：`authenticated` 的计算考虑 needsTotp——`authUser && !needsTotp` 才报 true，否则进 false 分支暴露 needsOtp/needsTotp；get_server_status 同步 + 新增 needsTotp 字段
  - 取舍：不采用清空 authUser 的方案，避免破坏 get_weekly_report 等依赖 authUser?.id 的本地库查询
  - 实测：正常认证态无回归（authenticated:true + needsTotp:false）；needsTotp 态两处正确报 authenticated:false + needsTotp:true

**演进意义**：状态透出的真实性问题——「内部处于待验证态」但「外部状态接口说正常」会让上层 Agent 基于错误前提做决策；确立 **状态接口真实性原则**：authenticated 必须反映真实可用性（待验证=未认证），宁可报 false 也不要「看起来正常」；清空 authUser vs 计算修正的取舍记录为后续状态设计提供参考。

## 2026-08-20 · 发布 v2.3.0（好友域三件套 + 认证自愈链 + 作者维度查询）

**发布**：v2.3.0（+12 个 MCP 工具）——好友域能力补齐（资料变更追踪 / 通知收件箱 / 收藏分组管理三件套，@CyberNekokoya 三个 PR）+ 认证自愈链收尾（TOTP 双通道 #45 → 运行期 401 自动重登录 #52 → needsTotp 状态透出 #60，@CcendercatcC 链路）+ 世界作者维度查询（#53）+ favorite_world 契约修正（#55，@Menaed）+ 端口占用预检（#50）+ 迁移补迁（#47）+ chill 过滤规则（#44，@Sand-85）。注：#44/#45/#47 于 v2.2.0 发布当日合并，其代码随本版首次可用（v2.2.0 tag 早于其合并）。

**关键改动**（v2.2.0 之后进 main 的 11 个 PR）：
- feat ×4：TOTP 两步验证登录 + submit_totp（#45）；get_worlds_by_author 按作者列全部世界 + get_world_name 补 authorId（#53）；好友资料变更追踪（#56）；通知收件箱 5 工具（#57）；好友收藏分组管理 4 工具（#58）
- fix ×5：运行期 API 401 自动重登录 TOTP 免重启（#52）；needsTotp 状态 /health 与 get_server_status 透出（#60）；favorite_world 的 favoriteId 传对象 ID（#55）；8799 端口占用预检前置（#50）；migrate 补迁 gamelog_location 自己位置历史 + 显式 userId 迁移 0 条（#47）
- docs ×2：chill/放松向世界过滤规则入 skill（#44）；pr-review 公共 skill 新增 + review-workflow 重命名扩展覆盖 issue 审核（直推 main）

**演进意义**：本版确立三条能力线——①**认证自愈链完整闭环**（登录双通道 → 运行期自愈 → 状态真实透出），TOTP 账号从此全生命周期免人工干预；②**好友域从只读到可操作**（资料变更可查、通知可处理、收藏可管理），Agent 对好友关系的管理能力首次超过客户端手动操作；③**世界查询补齐作者维度**（按作者列图），为「给作者全部图加权」类工作流提供前置能力。工具数 77 → 89（+12，无移除），版本引用点 package.json / package-lock.json / hermes-plugin/plugin.yaml 三处同步。

## 2026-08-20 · feat(companions): 好友对同屏/见面分析（PR #61 + #62 @CyberNekokoya，已合并）

- `#61` (MERGED, 2026-08-20) feat(companions): get_friend_pair_screen 新增同屏总时长与按世界拆分时长
- `#62` (MERGED, 2026-08-20) feat(companions): 新增 get_friend_pair_meeting 查询好友对每次见面的时段与时长
  - 需求：get_companions 只回答"和谁一起玩过"，回答不了"一起玩多久、什么时候一起玩"——好友对级别的共玩分析缺失
  - `#61` get_friend_pair_screen（core/storage.js findFriendPairScreen，+128 行）：对好友 B 的每条可识别实例事件，找好友 A 在同一实例且时间戳在 ±windowMinutes（默认 30）内的匹配，计一次同屏；排除 offline/traveling/private（private 无房主信息无法判定同房，不同人不同时间的 private 会被误判为同一房间——实测坑）；返回 matchCount（次数）、totalMinutes/totalSeconds（总时长，段首到段尾累加）、worldDuration（按世界拆分时长）、worlds（共现世界）与 matches；批量查世界名避免 N+1
  - `#62` get_friend_pair_meeting（findFriendPairMeetings，+77 行）：同一实例内所有同屏匹配事件合并为一次见面（meeting），返回每次的 start/end/durationMinutes、世界与实例；meetingCount + totalDurationSeconds；userIdA ≠ userIdB 显式校验
  - 登记同步：mcp-definitions.js / rpc-router.js / handlers/events.js / AGENTS.md / SKILL.md 工具表同步新增

**演进意义**：好友共玩分析从"和谁玩过"（get_companions 计数）深化到"玩多久、按世界拆、单次见面时段"——friend-location 是位置快照，无法可靠重建在场区间，确立**快照事件对匹配口径**（B 事件 × A 同实例 ±window 内事件 = 同屏，private 一律排除避免误判同房）；分析型工具的分层（聚合统计 screen / 明细列表 meeting）为上层社交分析（"谁是真·常驻玩伴"）提供两级粒度，同一数据源零新增采集。

## 2026-08-21 · feat(totp): TOTP 自动重新登录（配置 totp_secret 全自动）

- 需求：使用者提出 TOTP 账号目前必须手动调用 `submit_totp` 提交验证码才能登录，无法无人值守，希望实现自动重新登录
- 新增 `core/totp.js`：RFC 6238 TOTP 生成（纯 Node crypto 零依赖，符合跨平台/低配约束），支持 otpauth:// URI 与 base32 密钥解析、时钟窗口容错（前后窗口）
- `credentials.json` 新增可选 `totp_secret` 字段；`vrchat-api.js` 新增 `setTotpFetcher`，`_doEnsureAuthWithAutoOtp` 的 needTotp 分支（含邮箱失败兜底分支）自动生成验证码完成登录；自动失败保留 tempAuthCookie 转手动 `needsTotp` + 30s 冷却等待下个窗口防循环
- 启动 / 运行期 401 / WS 重连三条路径统一复用自动链；`/health` 暴露 `auth.totpAutoEnabled`；`submit_totp` 保留为手动兜底
- 实测：`node test-totp.mjs` 27/27 通过（RFC 6238 附录 B 官方测试向量 + URI/base32 解析 + 窗口容错 + 无效输入）；配置 secret 后启动自动登录验证通过

**演进意义**：认证自愈链从「邮箱 OTP 自动 + TOTP 手动」升级为「邮箱 OTP / TOTP 双自动」——TOTP 账号从"至少一次人工介入"变为真正无人值守；RFC 6238 本地生成（纯 JS 零依赖）与 VRCX 的 Authenticator 自动登录行为对齐，确立 **2FA 全自动原则**：所有验证类型配置凭据后可完全自愈，人工 `submit_totp` 仅作为失败兜底。

## 2026-08-21 · feat(notify): 登录状态主动通知——需人工介入/异常时提醒宿主（issue #69）

- 需求：issue #69 提出 vrc-monitor 是无人值守后台服务，登录结果只写日志，TOTP 待验证 / OTP 抓取失败 / 401 重认证失败时宿主在 VR 里无感知，服务「静默失效」；希望只在需人工介入/异常时主动通知（正常自动登录不通知）
- 新增 `core/notifier.js`：登录状态通知中心，`notifyAuth(kind, message)` 统一入口，kind ∈ needsTotp / otpFailed / reauthFailed / recovered；**多次失败聚合去抖**（otpFailed/reauthFailed 连续失败达 `consecutive_fail_threshold` 且距上次同类型超 `min_interval_sec` 才通知，状态跳变型 needsTotp/recovered 同消息去重）；通道可插拔注册，始终保留日志
- 新增 `core/notify-channels.js`：跨平台通道——desktop（Linux notify-send / macOS osascript / Windows PowerShell toast）+ webhook（POST JSON 到 webhook_url）；desktop 内部 fail-safe（通知守护缺失/DBUS 不可用时静默降级不崩服务）
- 新增独立配置 `notify-config.json`（+ `notify-config.example.json`，已 gitignore）：与 credentials 分离，默认关闭，缺文件或 enabled:false 不影响服务
- 接线 4 处：启动登录失败（start-monitor.js）、WS 重连 2FA/认证失败（ws-manager.js）、运行期 401→needsTotp（rpc-router.js）、submit_totp 恢复（handlers/auth.js，闭合「正常→异常→恢复」循环）
- **基于 #70（TOTP 自动登录）开发**：notify 分支合入 #70 代码，start-monitor.js / ws-manager.js 的 TOTP 自动登录逻辑与通知逻辑兼容共存（needsTotp 时按是否配置 totp_secret 区分提示文案）
- 实测：去抖逻辑单元验证通过；webhook 本地 mock 真实 POST 成功；集成启动确认「🔔 登录状态主动通知已启用」+ TOTP 自动登录保留 + 登录/WS 正常；desktop 通道在无通知守护环境静默降级不崩服务（真实弹窗需系统通知守护，Linux 端由使用者/上游在有 dunst/mako 的环境验证；Windows PowerShell toast 交上游实测）

**演进意义**：认证自愈链（#45→#52→#60→#70）最后一块拼图——自愈链只能自动处理无需人参与的环节，一旦需人工介入必须有主动提醒，否则自愈等于没救。确立 **通知即感知原则**：无人值守服务在「正常→异常→恢复」状态跳变时主动外发，只在需人介入/异常时通知避免噪音，跨平台通道可插拔 + 独立配置默认关闭，把「服务静默失效」变为「可感知状态」。

## 2026-08-20 · refactor(companions): 同屏匹配公共 helper 抽取 + 输出精简（PR #63/#67 @CyberNekokoya，已合并）

- `#67` (MERGED, 2026-08-20) feat(companions): get_companions 默认不返回 userTimeline，支持 includeTimeline 按需取全量
- `#63` (MERGED, 2026-08-20) refactor(companions): 抽取好友对同屏匹配公共 helper，get_friend_pair_screen 支持 matches limit
  - 痛点：`get_companions` 每次全量返回 userTimeline（数百上千条位置事件），时间窗口大时 MCP 输出 2MB+ 被截断，Agent 只能回退直读 SQLite；#61/#62 的 getEvents/parse/世界名查询近乎逐字重复，审阅建议抽公共 helper
  - `#67`：`findCompanions` 新增 `includeTimeline` 参数（默认 false，返回结构保持稳定不破坏调用方），mcp-definitions.js + SKILL.md 同步说明
  - `#63`：`core/storage.js` 新增 `_collectPairScreen` / `_resolveWorldNames` / `_getDisplayNames` 三个公共 helper，`findFriendPairScreen`/`findFriendPairMeetings` 复用消除重复；`findFriendPairScreen` 新增 `limit` 参数（仅限 matches 条数，不影响 matchCount/总时长统计口径）；SKILL.md 补「含实例内中途断开空档」口径说明
  - 实测：重构后 matchCount=31 / totalMinutes=196.4 / matches=31 与重构前一致无回归；limit=5 时 matchCount 仍 31、matches=5

**演进意义**：**输出量即可用性**——分析型工具的明细字段（userTimeline/matches）会随数据规模爆炸，默认全量返回导致 MCP 输出被截断反而不可用，确立「默认精简 + 按需全量」的参数设计模式（includeTimeline / limit 只裁剪明细、不改变统计口径），兼顾 Agent 可用性与统计正确性；重复代码抽取公共 helper 是审阅迭代的标准动作，「同屏匹配」沉淀为单一实现后口径修正只改一处。

## 2026-08-20 · refactor(world-names): 统一世界名解析公共模块 + 负缓存清理（PR #64/#68 @CyberNekokoya，已合并）

- `#64` (MERGED, 2026-08-20) refactor(companions): 抽取 resolveWorldNames 公共工具统一世界名解析
- `#68` (MERGED, 2026-08-20) fix(world-names): 负缓存过期条目清理，防止长期运行累积失败 worldId
  - 痛点：`getWorldName` 的「缓存 → 缺失批量 API → 写回 → 失败处理」逻辑在多个 handler 重复实现（friends.js / events.js 周报 / recommend.js×2 / favorite-worlds.js / event-pipeline.js），行为不一致且缺统一负缓存（PR #36 W3：get_online_friends 对失败 worldId 每次调用都重试，浪费 API 配额）
  - `#64`：新增 `core/world-names.js` 公共模块——`resolveWorldNames(ctx, worldIds, opts)` 统一「本地 world_cache 命中 → 缺失串行 /worlds/{id}（throttleMs 节流）→ 成功 upsertWorld 写回 → 失败写进程内负缓存（5 分钟 TTL）」流程；friends.js（get_online_friends）与 events.js（周报 + 群组房）改用，补上 W3 负缓存缺口；favorite-worlds.js / recommend.js 保守范围不强改（避免破坏各自字段结构），未来可复用底层负缓存/节流
  - `#68`（回应 #64 审阅非阻断建议）：负缓存只按 expiresAt 判断失效、从不删除，长期运行会累积失败 worldId——读取时惰性删除过期条目 + `pruneNegativeCache()` setInterval 定期清理（5min 同频，`.unref()` 不 keep 进程存活）；有效条目行为不变（命中仍兜底不重发 API，配额保护不回归）
  - 实测：`resolveWorldNames` 缓存命中不调 API / 缺失走 API 写回 / 失败写负缓存二次调用不再发 API 全部符合预期；prune 50ms 短 TTL 复刻测试过期删除/清空/有效保留全部 PASS；重启后 MCP 实测 get_online_friends / get_weekly_report 世界名正确无回归

**演进意义**：**基础设施收敛**——同一能力（世界名解析）在 6 处重复实现且行为不一致，是典型的「隐式公共设施」：统一成单一公共模块后缓存/节流/负缓存策略只维护一份，修复一个 handler 的配额浪费问题（#36 W3）其他 handler 自动受益；负缓存清理补上「进程内缓存长期运行条目累积」的卫生死角，确立 **有缓存的模块必须配清理** 的健壮性原则。

## 2026-08-20 · perf(events) 复合索引 + fix(notifications) 分页语义（PR #65/#66 @CyberNekokoya，已合并）

- `#65` (MERGED, 2026-08-20) perf(events): 新增 world_id 与 (user_id,created_at,type) 索引加速查询
- `#66` (MERGED, 2026-08-20) fix(notifications): total 改为 returned+hasMore 明确分页语义
  - `#65` 痛点：events 表仅单列索引，无法覆盖 `WHERE world_id IN (...)` 与按用户+时间+类型复合查询，150MB+ 事件表全表扫描（findFriendPair* / getFriendGroupStats / getGroupHeat / getLatestFriendLocations 都受影响），随表增长越来越慢
  - `#65` 实现：`core/init-db.sql` 新增 `idx_events_world ON events(world_id)` 与 `idx_events_user_time_type ON events(user_id, created_at, type)`（CREATE INDEX IF NOT EXISTS 对存量库幂等生效）；15.7 万行副本验证创建成功，EXPLAIN QUERY PLAN 实测 `SCAN events` → `SEARCH USING INDEX idx_events_world`，性能提升显著
  - `#66` 痛点：`get_notifications` 的 `total: all.length` 是**当前页条数**（≤ limit）而非总通知数，做分页统计时误读为总数；VRChat API 不返回总数字段、无法获知真总数
  - `#66` 实现：移除误导性 `total`，改 `returned`（本页 API 实际返回条数）+ `hasMore`（取满 limit 推断可能还有下一页），保留 shown/limit/offset；mcp-definitions.js + SKILL.md 同步语义；另核实 ws-manager 回退 7892 与 fetch-x-worlds 直连为**文档化国内网络 WS 兜底设计**（AGENTS.md/README 已说明先直连、失败回退本地代理），非缺陷，本次不改
  - 实测：get_notifications 返回 returned:0 / shown:0 / hasMore:false 语义正确，不再有误导性 total

**演进意义**：数据规模驱动的两个「语义诚实」修正——①**索引即 SLA**：功能正确但查询随数据增长劣化是事件型应用的必然阶段，补复合索引把「全表扫描」变成「走索引」，为分析型工具（同屏/热度/位置）的长期可用性兜底；②**分页字段语义诚实**：`total` 名不副实（是页数不是总数）会诱导上层 Agent 做出错误统计判断，改 returned+hasMore 后「不知道就是不知道」——与状态接口真实性原则同源：**接口字段命名也要真实**。

### 2026-08-21 · fix(notify): 修复 webhook 异步通道 rejection 逃逸导致进程崩溃（审核实测复现）

- **根因**：`core/notifier.js` 的 `_dispatch` 用同步 `try/catch` 调用通道，但 `makeWebhookChannel` 返回 async 函数——fetch 失败（网络不可达/超时/非 2xx）时 Promise reject，同步 catch 捕获不到 → unhandled rejection → Node 15+ 默认 throw 模式**直接终止进程**。webhook 是外部网络通道、最易失败，失败时反而带崩无人值守服务，与「通道失败静默吞掉」设计相反
- **修复**：`_dispatch` 内对通道返回值做 Promise 兼容——`ret.catch(...)` 挂 rejection 处理（async 通道）；同步通道返回 undefined 不受影响。审核建议的两种方案取更通用的一种（对任何未来 async 通道生效）
- **顺手改进（审核 🟡 建议）**：needsTotp/recovered 状态跳变型也受 `min_interval_sec` 限制——3 处接线（启动/WS/运行期 401）文案不同，同一故障流短时间多次不同文案通知会刷屏，统一间隔约束
- **实测**：模拟审核复现（async 通道 throw + 真实 webhook 到不可达地址）→ 进程不再崩溃，rejection 被捕获记日志；去抖改进（状态跳变间隔/同消息去重/聚合阈值）全部通过

### 2026-08-21 · fix(totp): 落实上游 #70 审核的三条 🟡 迭代建议

- **建议1：邮箱兜底分支补冷却**——`_doEnsureAuthWithAutoOtp` 的「邮箱 OTP 失败 + 自动 TOTP 兜底也失败」分支未设冷却，与仅 TOTP 分支不对称；补 `this._reauthCooldownUntil = Date.now() + 30_000`，等待下个 TOTP 窗口再自动重试（vrchat-api.js）
- **建议2：totpFetcher 利用前后窗口容错**——start-monitor.js 的 totpFetcher 由「仅当前窗口」改为「返回 [前窗口, 当前, 后窗口] 三窗口验证码数组」，`_autoTotpLogin` 依次尝试（任一成功即登录，容忍时钟漂移/窗口轮换；单值输入向后兼容）
- **建议3：parseTotpSecret 参数合法性校验**——otpauth:// URI 的 digits（6-8 位整数）、period（正整数秒）、algorithm（仅 SHA1/SHA256/SHA512）加入合法性校验，非法参数启动即报错回退手动模式（core/totp.js）
- **实测**：parseTotpSecret 校验 11 项（非法 digits/period/algorithm 拒绝、合法通过）；三窗口容错 3 项；`_autoTotpLogin` 四场景（第2窗口成功/全失败抛错/单值兼容/无效码跳过）；双失败冷却补全验证 30s 生效；集成重启确认「TOTP 自动登录已启用（前后窗口容错）」+ 登录/WS 正常

## 2026-08-21 · docs: AGENT-REVIEW 规范补充「请求再次审核」触发动作（commit 615997a，直推 main）

- 痛点：多 Agent 协作审核流程中，作者修复完成、推送新 commit 后**没有显式动作触发审核方复测**——审核方只能靠轮询发现新 commit 就复测，但作者可能仍在演进中间态，导致对未完成 commit 空转；反之作者推了修复 commit 但没发任何信号，审核方不知道「这轮修完了、可以测了」
- AGENT-REVIEW.md 新增 §2.7「请求再次审核（作者侧触发动作）」：作者完成修复推送新 commit 时，**必须在同一 PR 评论发一条「请求再次审核」**——格式首行 `[AGENT-REVIEW] 请求再次审核 #<N>：已按 REQUEST_CHANGES 修复，commit <sha>`（沿用 `[AGENT-REVIEW]` 认领标记前缀保证机器可解析，正文含 PR 编号 + 修复 commit sha 便于定位增量）
- 审核方以该评论为**唯一复测触发信号**：只推 commit 未发请求评论 = 作者尚未声明修复完成（可能仍在演进），审核方不做复测，避免对中间态空转；每响应一轮 REQUEST_CHANGES 都要重复「修复 → 请求 → 复测」循环直至 APPROVE
- DEVELOPMENT.md §14 同步：commit 信息/PR 描述/issue 正文的 Agent 口吻规范后新增「响应审核后必须请求再次审核」，与 AGENTS.md / DEVELOPMENT.md §2-14 同步生效

**演进意义**：把「复测触发」从隐式约定（轮询到新 commit 就测）升级为**显式协议动作**（作者声明修复完成才测）——解决多 Agent 异步协作中「空转中间态」与「修复完成无信号」的双向失配，确立 **声明式复测触发原则**：谁修复谁负责宣告「可以测了」，审核方只对声明过的状态消费复测算力；`[AGENT-REVIEW]` 前缀机器可解析，为未来自动化复测队列铺路。

## 2026-08-21 · docs: 全仓文档漂移审查修复（PR #72 @CyberNekokoya，已合并）

- `#72` (MERGED, 2026-08-21) docs: 文档漂移修复（架构图/工具数/快速开始/日期/skill 误报）
  - 前置序列：e11b10f「应用文档漂移修复审查产物」→ 5f3cd3b Revert（审查中间态未直接采用）→ c14c8ae 最终合并（21:13）
  - ARCHITECTURE.md 全面对齐实现：core 模块行数全部修正为实测值（storage 805→1508、mcp-definitions 640→1056、rpc-router 341→538 等 12 处），补齐 9 个缺失模块（recommend-worlds / fetch-x-worlds / world-names / theme-config / totp / notifier / notify-channels / vrcx-db-paths / init-x-worlds.sql），handlers 表 7→16 个文件，Schema/数据流图补 5 张表，start-monitor.js 分区按实际 357 行重写，Agent Skill 数量 2→7
  - 演进记录按版本实测对齐工具数：v1.7.0（18→27，修正 hash 为 6b9050c）、v1.14.0（48→49）、BOOTH 三连（60→62→62→66）；#70/#71 提交日期修正为 08-21；INDEX 里程碑补 08-20/08-21 两条；groups.js 导出函数数 9→10
  - README 三语（中/en/ja）快速开始：IMAP 邮箱不再写为刚需（邮箱 OTP 或 TOTP 二选一）、凭据步骤补 TOTP 通道、健康检查字段改为真实 JSON；en/ja 补 service-linux 导航行、修正失效 skill 名（vrc-monitor-companion-query → vrchat-social-queries）
  - AGENTS.md 工具清单格式修复（悬空 5 个工具并入括号列表）；scripts/check-doc-drift.py 自身修复（转义消除 SyntaxWarning、skill 提及正则限同行消除 `skills/connected` 误报）
  - 验证：check-doc-drift.py 全项通过无漂移、无 WARN；工具数均按 `git show <tag>:<file>` 实测统计

**演进意义**：**文档治理闭环的第二次全仓漂移清扫**——上一轮（v2.1.1 文档治理）确立「文档要与实现一致」，本轮把执行标准推到**数字级一致**：行数/工具数/模块数全部按 git 历史实测对齐（`git show <tag>:<file>`），不凭记忆写数字；同时**审查中间态先 Revert 再修**（e11b10f 被 5f3cd3b 回退、c14c8ae 才落地），文档修复同样走「提交→审查→修正」闭环而非一次成型；顺带修复漂移检测器自身（SyntaxWarning + 误报），确立 **检测工具本身也要被审查** 的原则。

## 2026-08-21 · fix(plugin): vrc_restart 假成功不重启（commit 06bf4cb，直推 main）

- 根因（两层）：① restart() 用 try/except 吞掉 stop() 失败（stop 返回 ok:False 不抛异常），start() 探测到服务还在运行就返回 already_running——**代码变更从不生效但看起来成功**；② stop() taskkill 后只等 3 秒不验证进程是否真退出，进程仍存活也返回 ok:True（假成功），restart() 等端口循环跑满后无条件 start()
- 修复（hermes-plugin/process_manager.py，+88/-9）：stop() taskkill 后检查 `_pid_alive`，进程仍存活返回 ok:False + 明确错误；restart() 等端口循环改 for-else，health probe 持续响应（旧进程没死）即返回错误中止，不再调 start() 造成 already_running 假象；`_find_monitor_pid()` 升级为 netstat（127.0.0.1:8799 LISTENING 定位 pid）+ wmic 命令行匹配**双方法**，解决 inferred 状态（.active.json 无 pid）定位不到真实 pid；stop() 在 state 文件缺失时也尝试定位真实 pid
- 兜底：state 缺失 + 探测到服务在跑但定位不到 pid 时，明确报「请手动 taskkill 或重启 Hermes」，不静默返回成功

**演进意义**：**运维动作的「效果验证」原则**——服务管理命令不能只看命令本身执行了没有，必须验证命令的**效果**（stop 要确认进程真死、restart 要确认旧进程确实退出后才启动新的）；「假成功」比「失败」更危险：失败会报警，假成功会让用户以为代码变更已生效而实际从未重启，问题被长期掩盖。确立 **进程管理双向验证**（kill 后验证死、启动前验证端口释放）+ **失败必须显式中止**（restart 任一步失败不继续伪装成功）的健壮性模式，状态文件缺失时用端口监听兜底定位真实进程。

## 2026-08-22 · 世界知识库完整性三连：主题标签匹配 + sleep 手动标记 + 信息回填（PR #74/#75/#77 @CyberNekokoya，已合并 2026-08-22）

- `#74` (MERGED, 2026-08-22) fix(world): 主题/安静图关键词匹配纳入 tags（Closes #73）
- `#75` (MERGED, 2026-08-22) feat(world): 新增 set_world_sleep 手动标记睡觉图工具
- `#77` (MERGED, 2026-08-22) fix(world): 兜底插入世界回填信息字段，修复 world_kb 信息恒空（Closes #76）
- 同源审计：issue `#78` (CLOSED, 2026-08-22, PR #80 合并 97b4d1a0) 讨论 world_kb.source 死列——从未被写入也从未被读取，全部 57 行恒为默认 'new'，DDL 注释声称的双语义（new=新发布推荐/hot=热门追加）从未实现；方向 A 删除（幂等迁移 DROP COLUMN + 去注释）/ 方向 B 补全实现（scan_new / manual 枚举）；提示与 events.source（活的，websocket/migrate/api_poll）同名易混淆。最终按 issue 共识方向 A 落地：init-db.sql 删列 + storage 幂等迁移（PRAGMA 判列存在再 DROP COLUMN，存量库重启自动清理）
- 背景链条（同一使用者部署后的反馈链）：手动标记名不副实睡觉图「鱼罗QIE」（名字无睡眠关键词但 API tags 含 author_tag_sleep）→ ① recommend_join 与 recommend_worlds 的主题/安静图匹配只测 name/description，漏掉 author_tag_* 标签（issue #73 → PR #74）；② world_kb.sleep_ok 列是预留空列、没有任何 MCP 工具能手动写入（PR #75）；③ 审计 world_kb 发现兜底插入（rate_world/mark_world_visited/add_to_backlog）的信息字段 100% 恒空——实测 57 行 world_name/author_name/created_at 全空，新图加权（FRESH_DAYS 窗口）对靠手工标记进表的世界永远失效（issue #76 → PR #77）
- `#74` 实现：core/recommend-worlds.js 新增 `buildMatchText(c)`（name+description+tags）统一三处文本来源（主题评分/theme 软过滤/excludeTheme 判定，消除两处文本来源不一致），recommend.js 的 `isQuietWorldName` 纳入 worldTags（命中 author_tag_sleep）；保留 sleep_ok 人工语义兜底，关键词与人工语义互补，不下沉统一正则避免扩大改动面
- `#75` 实现：storage.setWorldSleep 幂等 upsert（复用 rateWorld/markWorldVisited「世界不在表里插兜底行」模式，sleep_ok 列已存在无需迁移）+ misc.js handler + mcp-definitions/rpc-router 登记；本地数据不触达 VRChat 云端；文档同步 SKILL.md 工具表 + AGENTS.md 采样列举
- `#77` 实现：storage.backfillWorldKbInfo 仅在对应列空时回填（CASE WHEN COALESCE(col,'')=''，幂等不覆盖真实值）+ `ensureWorldKbInfo`（优先本地 world_cache 省 API → 缺失串行调 /worlds/{id} 限流 → 回填 cache 与 kb，任一步失败不阻断主操作）；rate_world/mark_world_visited/add_to_backlog 三处接入改 async，返回的 worldName 采用回填后值；实测「凄美之地3」world_name 由 '' 回填为「凄美之地3」、author_name 回填「元气克星花阳」，「鱼罗QIE」world_name/author_name（齊 iii）同步回填
- `#80` (MERGED, 2026-08-22) fix(world): 移除 world_kb.source 死列（issue #78 方向 A）——init-db.sql 删列定义及误导注释 + storage 删旧建列迁移块、新增幂等 DROP COLUMN 迁移（PRAGMA 判列存在再 ALTER，存量库重启自动清理）、修正 tags/description 注释；无写入/读取路径受影响（source 无任何写入方，recommend-worlds.js 的 `source: 'local'/'planet'/'official'` 是函数内临时字段非本列）
- 三 PR 均已验证：node --check 通过、单测全绿、重启服务 /health authenticated:true + ws.status:connected、真实 MCP 调用 + SQLite 确认生效；#74/#77 未新增工具、#75 新增 set_world_sleep 一个工具（check-doc-drift.py 无漂移）

**演进意义**：**本地知识库的「数据真实性」审计潮**——一次部署后的手动标记操作，牵出本地 world_kb 一整片「假数据」：信息字段 100% 为空、source 死列、预留空列 sleep_ok 无工具可写、推荐匹配漏掉官方/作者标签。确立三条原则：① **兜底插入也必须保信息完整**——任何写入路径都不得留下永久空字段，推荐系统新图加权依赖的 created_at 必须真实，否则「靠手工标记进表的新图永远不吃新图权重」；② **关键词匹配的文本来源必须包含 tags**——author_tag_* 是名字/简介不含关键词时的唯一语义来源，主题识别只测 name/description 是「名不副实」缺陷的结构性根因；③ **死列要么活要么删**——#78 把「从未实现的双语义列」摆上台面讨论，避免带误导性注释的空列长期潜伏（与 #66 的「接口字段命名也要真实」同源）。反馈链由同一使用者部署驱动：三 PR（#74/#75/#77）已合并（2026-08-22），同源 issue #78 → PR #80 已合并落地。

## 2026-08-23 · 发布 v3.0.0-beta（插件化架构重构）

**发布**：架构大版本（beta）。核心从「三件套集中式工具注册」（`mcp-definitions.js` + `rpc-router.js` + `handlers/*`）迁移为**插件式架构**，工具数 91 → 92（+`set_world_sleep`）。行为兼容非重构基线（91 工具逐字节一致），调用方式与返回结构不变。

**关键改动**：
- 工具自声明 + 注册表自动生成（PR-1 #88）：每个工具改为在所属文件内自声明 `def`，新增 `core/registry.js`（registerTool/dispatch/listTools），按 `core/tool-order.json` 顺序输出，移除旧三件套
- safe-mode 子系统（PR-1 #88 承接 #84 语义）：`core/safe-mode.js` 精确维护 `DESTRUCTIVE_TOOLS`，`VRC_MONITOR_SAFE_MODE=true` 时 tools/list 过滤 + tools/call 拦截同一路径
- 插件化落地（PR-2 #89）：`core/plugin-loader.js`（静态扫描/依赖拓扑/热加载回滚/失败隔离）+ `core/plugin-api.js`（6 面 API：registerTool/db 命名空间/vrchat.fetch 委托/log/tools.call+has/provide+consume+hasService）；8 个官方插件（booth/favorites/groups/media/planet/recommend/world-kb/x-creators）；35 个核心服务（storage 14 + x 6 + world 8 + recommend 7）经 `api.consume` 暴露；契约 `docs/PLUGIN-API.md`（v1.1-experimental）
- 贡献模型切换（PR-3 #90）：DEVELOPMENT.md 功能一律做成插件 / core 只收 fix 与底座演进；插件模板 `docs/plugin-template/`；社区插件索引 `plugins/index.json`；CI（Node 18/20/22 × Ubuntu/Windows 无凭据冒烟）；AGENT-REVIEW 索引 PR 简化审核；**解冻公告**（功能类 PR 冻结期随 PR-3 解除）
- 安全模式 #84（Menaed）：`VRC_MONITOR_SAFE_MODE=true` 自动移除破坏性工具（误删防护）
- world 域增强（#74/#75/#77/#80）：主题/安静图关键词匹配纳入 tags；新增 `set_world_sleep` 手动标记睡觉图；兜底插入信息回填（world_kb 信息恒空修复）；移除 world_kb.source 死列

**演进意义**：**从「中央注册表」到「插件花园」的架构转身**——旧模型所有贡献者在 `mcp-definitions.js`/`rpc-router.js`/`handlers/*` 三个中央文件汇合，冲突与审核负担随工具数指数增长（storage.js 1508 行 + mcp-definitions.js 1056 行已不可逆膨胀）。插件化后核心收敛为「认证/WS 采集/存储/MCP 传输」稳定底座，功能以插件形式存在（自声明工具、`api.*` 交互、`plg_` 前缀 DB 命名空间隔离），自用插件放 `plugins/local/`（gitignore，免 PR 免 git），分享走 `plugins/index.json` 一行索引。确立三条原则：① **功能一律进插件，核心只收fix**——阻止核心继续腐化；② **插件只准通过 `api.*` 与核心交互**——不 import core/、不触碰 ctx、不直连 DB，可静态扫描校验；③ **契约一旦合并即冻结（只增不改）**——PLUGIN-API.md v1.1 的兼容性承诺。行为等价底线（91 工具逐字节一致）与非重构基线严格对齐，是插件化的**正确性验收基准**。

## 2026-08-24 · storage 底座重构：从 god class 到「底座 + 域模块」+ 文档漂移修复

**主题**：延续 v3.0.0-beta 插件化（PR-1/2/3），对核心存储层做**行为等价拆分**，并同步修复插件化后浮现的文档漂移。

**关键改动**：
- **storage 行为等价基线**（be493e1）：新增 `test-storage-snapshot.mjs` 对 storage 全部公共方法做「输入→返回」快照（62 探测点；临时库+固定时间戳+now 时间戳归一化，确定性可复现）；`--generate` 生成 golden / `--check` 校验，**接入 CI smoke**，每 PR/push 自动守卫
- **收敛外部裸 SQL 耦合**（2279cdf）：Storage 新增公开原语 `query/run/get/exec/transaction/backup`，把 core/plugins 里 15 处 `storage._query/_run` + 11 处 `storage.db` 裸访问改为公开原语（消除 `_query/_run/.db` 封装泄漏；`_normParams` 裸键透传 + better-sqlite3 命名参数匹配保证行为等价）
- **抽社交分析域**（d6da42e）：`findCompanions/screen/meetings/onlinePattern/weekly 等 7 公共+3 私有方法` 拆到 `core/analytics/social.js`（`SocialAnalytics` 构造注入 storage），Storage 留 7 个薄转发
- **按域拆 4 存储模块**（912fbe2）：world(16)/cache(12)/contacts(5)/x-worlds(6) 共 39 个方法拆到 `core/domains/{world,cache,contacts,x-worlds}-store.js`，Storage 留薄转发保持 `storage.X` 服务面
- 文档漂移修复（b0de7f5/86d440f/ec5410c/54efa5a）：三语 README 对齐 #95「Node ≥22」；`plugins/index.json` 残留「91 个 MCP 工具」（违反去数字政策+计数过时）清除；en/ja README 文档导航补 PLUGIN-API/PLUGIN-DEV 行、MCP 段补插件分层；插件零依赖契约 Node 口径统一 ≥22；review-workflow skill 补 review.json 裸控制符 Pitfall；`check-doc-drift.py` 数字残留扫描纳入 `plugins/`+`docs/`（跳过 `docs/history/` 点时间快照），test-registry/dump-tools 注释 91→92

**storage.js 行数**：1582 → 523（−67%）。

**演进意义**：**「行为等价验收」取代「逐字节回归」的大文件拆分方法论**——god class 拆解最大的风险是行为漂移，本批确立先建**快照基线**（输入→返回 golden）再动刀，每步 `node test-storage-snapshot.mjs --check` 对拍并接入 CI，保证 62 个公共方法输出逐字节一致。拆法采用**转发式**（逻辑搬到独立模块、构造注入底座 `this.storage`、Storage 留薄转发保持公共 API 面）→ 插件 `api.consume('storage.X')` 与核心工具直调**零外部 churn**（不改任何调用点），快照天然守护。架构上把 storage 从「一个类管所有域」（与 mcp-definitions.js 同源的 1582 行膨胀）收敛为「底座（init/原语/事件/好友）+ 分析域（social）+ 4 个存储域模块」，各域独立可单测。侦察还纠正了原预案判断：`group_cache`/`booth_items`/`world_kb`/`planet_cache` 等表**跨域共享**（core events 工具直读 group_cache、recommend 插件 consume `world.*`/`x.*`），故**不搬到插件私有表**（会打断共享消费方），改为「核心底座保留表 + 按域模块化组织」。

## 2026-08-25 · 发布 v3.0.0（正式版 · 插件化架构大版本转正）

**发布**：架构大版本正式版。v3.0.0-beta（08-23）转正为稳定版，工具数维持 92；插件体系、贡献模型切换、storage 底座重构全部达成正式状态，行为兼容基线（91 工具逐字节一致 + 新增 set_world_sleep）持续守住。

**关键改动**（相对 v3.0.0-beta）：
- **X 源头浏览器抓取（issue #82/#87，PR #96）**：Nitter/SearchTimeline 2026 双源失效后引入 Playwright 浏览器抓取，补齐 Anubis challenge 处理 + 通道自动探测 + env 时序重构；双数据源降级（主力失败自动切备源）修复单源漏抓
- **Node 22 对齐（#95）**：engines 与 CI 矩阵从 18/20 收窄到 22（匹配插件零依赖契约口径）
- **groups 增强**：群组公告自动联查发布者真实用户名（authorName）
- **world 体验**：逛过即自动移出待逛列表（visited 首次置位同步清 backlog，#91）
- **CI 加固（#93）**：dump-tools 计数断言去硬编码，行数与 tool-order.json 动态对齐
- 文档漂移修复（b0de7f5/86d440f/ec5410c/54efa5a）：三语 README Node≥22、plugins/index.json 去工具数、drift-check 数字残留扫描纳入 plugins/+docs/

**演进意义**：**Beta 转正是「架构转身的验收节点」**——3.0.0 把 v2 的「三件套中央注册」（mcp-definitions/rpc-router/handlers）重构为「插件花园」（工具自声明 + 注册表 + 8 官方插件 + 贡献模型），beta 到正式期间补齐了外部贡献流程（PR-1/2/3 三阶段）、storage 底座行为等价拆分、X 数据源健壮性（浏览器抓取）、运行时对齐与 CI 去硬编码——这些不是演示性重构的锦上添花，而是让「插件化」长期可持续的收尾动作。确立原则：**架构大版本必须经 beta 实战验证贡献流程与行为等价基线，才可转正**；core 只收 fix 与底座演进、功能一律进插件的边界从此被正式版本锚定。

## 2026-08-25 · Windows 常驻服务静默化：pythonw.exe 探测优先（PR #97 @psenY，已合并）

- `#97` (MERGED, 2026-08-25) fix(service-windows): 优先探测并使用 pythonw.exe 解决计划任务每分钟控制台窗口闪烁（23+/8-，单文件 service-windows/setup-windows.cmd）
- 背景链条：PR #30（2026-08-20 合并）引入 Windows 常驻服务（service-windows/：开机自启/崩溃自愈/每日修复报告），部署者使用 setup-windows.cmd 后反馈**系统每隔 1 分钟瞬时弹出黑色 CMD 控制台窗口并关闭**，严重干扰日常操作
- 根因：计划任务 VrcMonWatchdog 用控制台解释器 python.exe 注册，Windows 任务计划程序在交互桌面会话拉起控制台程序时每次执行都闪现窗口；另含 `if errorlevel 1` 括号块内 `%STARTUP%` 延迟扩展问题、未声明 chcp 65001（多语言终端下非 ASCII 解析报错）
- 实现：动态探测同目录/PATH 下的 pythonw.exe（GUI 无窗口子系统解释器），存在则优先、缺失静默回退 python.exe；VrcMonWatchdog / VrcMonLauncher 登录自启 / VrcMonDailyReport 三任务统一 pythonw.exe 静默调用；调整 STARTUP 初始化顺序 + VBS 生成块加固；脚本开头声明 chcp 65001
- 验证：Windows 11 端到端实测，计划任务成功指向 pythonw.exe，多周期（每分钟触发）观察零窗口闪烁；无 pythonw.exe 环境正常回退、不破坏现有行为

**演进意义**：**「无头守护」在 Windows 平台的部署体验补丁**——Linux systemd（#42）天然无窗口，Windows 计划任务常驻的「每分钟弹窗」则是部署者天天可见的干扰：功能正确但观感致命，每分钟打断一次，使用者宁可不用也不愿被反复弹窗。确立模式：**窗口化平台上守护进程必须用无窗口解释器（pythonw.exe）启动**，且「探测优先 + 静默回退」保证不破坏既有部署；顺带修复延迟扩展与编码声明两个「只有真实部署才会暴露」的批处理健壮性问题。已合并（2026-08-25），补上 Windows 托管侧的最后一块体验短板（与 #42 Linux systemd 形成双平台常驻闭环）。

## 2026-08-25 · 发布 PR #98：auth-guard 公网鉴权插件 + Docker 容器化部署（@psenY，已合并）

- `#98` (MERGED, 2026-08-25) feat(plugins): 新增 auth-guard 访问鉴权插件与公网安全防护机制（+558/−19/17 文件，合并 commit ad93f1f）
- 需求来源：使用者将监控服务常驻路由器/NAS/VPS（iStoreOS、OpenWrt、Docker 容器），需监听 0.0.0.0 映射公网以便外部随时经 MCP 端点交互；此前服务默认 127.0.0.1 仅本机可连，一旦放开绑定即无任何 HTTP 层鉴权，裸奔公网有被扫描/调用风险
- 实现（严格遵守 Plugin-First：功能进插件、核心只做底座演进）：
  - 官方插件 `plugins/official/auth-guard/`：注册 `http.authenticate` 全局服务（Bearer 头 / X-API-Key 头 / `?token=` 查询参数三方式提取令牌），`crypto.timingSafeEqual` 常量时间比对防侧信道；**未配置 Token 时 100% 默认放行**，本地单机零配置体验不变
  - 3 个 MCP 安全工具（工具数 92 → 95）：`auth_get_status`（监听 IP/端口/Token 启用与公网就绪度评估）、`auth_generate_token`（32 字节高强度随机 Token + 可直接写入 .env 的配置片段）、`auth_verify_token`（校验指定 Token 是否匹配生效配置）
  - 核心底座支持：core/http-server.js 全局鉴权中间件 + URL pathname 解析；start-monitor.js 新增 `VRC_MONITOR_HOST`（默认 127.0.0.1）/`VRC_MONITOR_PORT`（默认 8799）；plugin-loader 补 hasService/consume 辅助
  - **Docker 容器化**：Dockerfile + docker-compose.yml + .dockerignore（NAS/软路由/服务器一键容器化，数据卷持久化 data/backups、credentials.json 只读挂载、`.env` 环境变量透传）
  - 审查闭环：合并前修复两个阻断项——`.dockerignore` 排除凭据/密钥防镜像泄露 + HEALTHCHECK + 端口校验（d625ce7）、start-monitor 补齐 PORT/HOST 常量（c4b4deb）
- 验证：test-auth-guard.mjs 14/14 全过（Token 提取/多 Header 格式/时序比对/生成校验逻辑/端到端中间件：无 Token 放行、错 Token 401、三方式携带成功访问 /health 与 /mcp）；三语 README + 插件索引登记

**演进意义**：**「公网裸奔」到「零配置鉴权 + 一键容器化」的安全补齐**——服务跑在本机时 127.0.0.1 天然安全，但 NAS/软路由场景必须监听 0.0.0.0 并映射公网，此前没有任何 HTTP 层防线，任何扫描到端口的人都能直接调用 MCP 工具（含破坏性工具，safe-mode 也只是默认未开）。auth-guard 确立三个模式：① **插件不只是工具，还可以注册全局服务（http.authenticate）**——安全横切面不侵入每个 handler，而是挂在 HTTP 中间件层，核心只提供挂载点；② **安全特性默认关闭、配置即启用**——未设 Token 完全放行保本地体验，设了才全接口 401 拦截，零迁移成本；③ **容器化是「部署形态」补全**——与 #42 systemd / #97 pythonw 一起，把 Linux 无头、Windows 常驻、NAS 容器三大部署场景全部闭环。同时这是插件化贡献模型（PR-3 #90）冻结解除后第一个完整走完「插件 + 核心底座扩展 + 文档登记 + 审查修复」流程的外部大功能，验证了 Plugin-First 从理论到实战的路径可行。

## 2026-08-26 · favorites 收藏夹整理三连（PR #99/#100/#101 @CyberNekokoya，已合并）

- `#99` (MERGED, 2026-08-26) feat(favorites): get_my_favorite_worlds 支持按收藏时间排序（sortBy=added）
- `#100` (MERGED, 2026-08-26) refactor(favorites): get_my_favorite_worlds 改经 /worlds/favorites 一次拉全，首调降至秒级
- `#101` (MERGED, 2026-08-26) feat(favorites): 新增 unfavorite_world 移除世界收藏工具
- 需求来源（同一使用者驱动）：收藏夹 worlds1/worlds2 两槽位已满（各 100 个），需腾挪到空槽位（worlds0/worlds4）按主题重新分组；整理需要三个能力——按收藏时间找到最新添加、快速列出全部收藏（而非等 15-20 分钟）、从旧槽位删除
- 实现要点：
  - #99：调研确认 `GET /favorites?type=world` 返回对象**不含收藏时间戳字段**、`/worlds/favorites` 的 sort 参数实测不生效，但 `/favorites` 返回列表本身就是**按收藏时间倒序（最新在前）**，与 VRChat 客户端 "Date Added" 排序一致（2026.1.2 补丁确认）→ 以列表天然顺序实现 sortBy=added
  - #100：`GET /worlds/favorites` 一次请求即返回全部收藏世界的完整详情（name/favorites/visits/popularity/occupants/capacity/tags/authorName/description），且返回顺序即收藏时间倒序（全量 286 个实测：7 种 sort 值返回顺序完全一致）→ 替代原实现逐个 `/worlds/{id}` 查详情建缓存的 15-20 分钟首调等待
  - #101：实测 `POST /favorites` 对已在其他槽位的世界返回 400「You already have that world favorited」——VRChat 不允许同一世界多槽位并存，移动必须显式 DELETE 旧记录；新增 `unfavorite_world` 补上「移除收藏」缺口（参照 unfavorite_friend 模式）
- 状态：三 PR 均已于 2026-08-26 合并（#100→#99（人工解冲突）→#101 顺序）

**演进意义**：**「数据足够快 + 操作能闭环」的收藏夹整理补课**——favorites 域此前有收藏/查询能力，但缺「取消收藏」和「快速全量列举」：整理收藏夹这种真实操作场景暴露出 15-20 分钟首调等待（逐个查详情）让工具形同不可用，且「移槽位」在 API 层面必须「删旧 + 加新」两步，缺删除就断链。确立模式：① **排序需求先查 API 天然行为再实现**——文档声称的 sort 参数不生效，实测发现列表本身按收藏时间倒序，直接利用而非发明新机制；② **「一次拉全」优于「逐条建缓存」**——/worlds/favorites 一次返回完整详情，把分钟级首调降到秒级，缓存策略让位于 API 能力；③ 操作闭环补齐——与 #25 favorite_world（收藏）配对，收藏夹从此「加得进、查得快、删得掉」。

## 2026-08-26 · favorites 世界收藏夹管理重置（VRC+ 专属收藏夹支持，本地 fork 改动）

- 需求来源（使用者提出）：**「无法管理 VRCPlus 专属收藏夹」**——favorites 域工具硬编码 `FAVORITE_TAGS = worlds0-4`，而 VRChat 世界收藏实际分两类 `type`：`world` 与 `vrcPlusWorld`（后者 VRC+ 专属，2026.1.1 起 +400 世界槽位）。现有 `favorite_world`/`unfavorite_world` 无法读写 VRC+ 分组，`get_my_favorite_groups` 用 `g.type === 'world'` 过滤器直接**丢弃** vrcPlusWorld 分组，且 `capacity` 从 FavoriteGroup 对象取（该对象**无此字段**，实测恒为 0/null）。
- 调研结论（实测当前账号 + 官方规范交叉验证）：
  - 分组 tag 是**动态的**：实测（VRC+ 订阅账号环境）world 类型 = `worlds0-3`（worlds0 已重命名 "chill"、worlds1 重命名 "Favorite Worlds 1"）+ vrcPlusWorld 类型 = `vrcPlusWorlds1-2`；而本仓库服务账号（**无 VRC+ 订阅**）仅 world 类型 = `worlds0-2`、无 vrcPlusWorld 分组——**硬编码 worlds0-4 既不覆盖实际、也缺 VRC+ 分组**；
  - `GET /auth/user/favoritelimits` 返回每类型容量（world 4×100、vrcPlusWorld 4×100、avatar 6×50、friend 3×150）——`capacity` 的唯一正确来源；
  - 分组管理端点：`PUT/DELETE /favorite/group/{type}/{name}/{userId}`（PUT 改 displayName/visibility；DELETE 语义=**清空组内全部收藏**，非删组）；`POST /favorite/groups` 创建分组**不存在**（分组是预定义槽位）。
- 实现（`plugins/official/favorites/index.js` 重构，插件优先、不碰 core 业务逻辑）：
  - **动态分组发现** `discoverWorldGroups()` 取代硬编码 `FAVORITE_TAGS`：合并 `GET /favorite/groups` + `GET /auth/user/favoritelimits` → 分组清单 `{tag, displayName, type, visibility, capacity, ownerId, id}`（world + vrcPlusWorld）；`worldTypeByTag()` 按 tag 前缀推断 type；`matchGroup()` 支持 tag 或 displayName 匹配（实测 "chill" 匹配到 worlds0）；
  - **修复** `favorite_world`：tag 校验改动态分组、POST `type` 按 tag 推断（**能写入 vrcPlusWorldsX**）、默认分组取 worlds0/首个；
  - **修复** `unfavorite_world`：`fetchAllWorldFavoriteRecords()` 合并拉取 world + vrcPlusWorld 两种类型（**能删除 VRC+ 专属收藏**）；
  - **修复** `get_my_favorite_groups`：返回 world + vrcPlusWorld 全部分组，`capacity` 改自 favoritelimits，补充 `id/visibility/usedCount`（usedCount 复用 `/worlds/favorites` 一次拉全统计）；
  - **增强** `get_my_favorite_worlds`：新增 `group` 参数按收藏夹过滤（tag 或 displayName）；
  - **新增 3 工具**（96 → 99）：`move_world_group`（世界分组间移动，删旧建新，官方客户端 2026.1.1 同款）、`update_favorite_group`（重命名/改可见性，PUT 端点，改 public 隐私敏感须 confirm）、`clear_favorite_group`（清空分组内收藏，destructive 加入 safe-mode `DESTRUCTIVE_TOOLS`）；
  - 登记：`core/tool-order.json` + `skills/vrc-monitor-agent/SKILL.md` 工具表 + `skills/vrchat-world-queries/SKILL.md` + AGENTS.md 采样列举。
- 验证：`scripts/check-doc-drift.py --json` → `has_drift: false`；`node test/test-registry.mjs` → PASS（99 工具，顺序+defs OK）；MCP 端到端实测（**VRC+ 订阅账号环境，全部临时数据并用后还原**）：`get_my_favorite_groups` 显示 vrcPlusWorlds1/2 + capacity=100 + usedCount；`favorite_world` 收藏「箱入り娘」到 vrcPlusWorlds2（`type: vrcPlusWorld`）→ `unfavorite_world` 删除还原；`move_world_group` 把 vrcPlusWorlds1 世界移到 vrcPlusWorlds2 再移回；`update_favorite_group` worlds2 改名→改回（displayName 匹配生效）；`clear_favorite_group` 预览（vrcPlusWorlds2 count=0 / worlds0 count=44）；结束复检云端状态完全还原（vrcPlusWorlds1 used=1、vrcPlusWorlds2 used=0、worlds2 显示名复原）。**无 VRC+ 订阅账号（仅 worlds0-2，如本仓库服务账号）降级行为**：`discoverWorldGroups()` 动态发现只返回存在的分组（world 类型），vrcPlusWorld 相关路径自然不出现，`favorite_world`/`unfavorite_world`/`get_my_favorite_groups`/`get_my_favorite_worlds` 均正常返回 world 分组，无需特判、不报错——动态发现逻辑天然兼容两种账号形态。

**演进意义**：**「VRC+ 专属收藏夹」从不可见不可写到完整管理**——上一轮 #99/#100/#101 把「加得进、查得快、删得掉」做到 world 类型，但这只覆盖了免费/默认槽位；VRC+ 用户实际还有 vrcPlusWorld 专属槽位（+400 世界）被硬编码 `worlds0-4` 完全挡在工具之外，等于「花了 VRC+ 的钱、工具却管不了 VRC+ 的收藏夹」。本次确立模式：① **收藏分组必须动态发现、禁止硬编码 tag 清单**——分组槽位由订阅（favoritelimits）决定且 tag 随历史演变（实测 worlds0-3 + vrcPlusWorlds1-2），硬编码必然漏类型漏槽位；② **type 是收藏的顶层维度**——`/favorites` 按 type 过滤、`/worlds/favorites` 混合返回，读写都必须按 type 路由，不能假设「世界收藏 = type=world」；③ **capacity 的唯一真相是 favoritelimits**——FavoriteGroup 对象不含容量字段，此前代码永远拿到 0/null；④ 分组管理（移动/重命名/清空）补全收藏夹整理闭环，与官方客户端 2026.1.1 的移动能力对齐。
## 2026-08-26 · 修复 refactor 后无 data/ 目录启动崩溃 + 旧数据迁移（PR #104，维护者修复）

- `#104` (MERGED, 2026-08-26) fix(storage): refactor 后无 data/ 目录时启动崩溃 + 旧数据不迁移（合并 commit d96b47b，关联 issue #103）
- 背景链条：refactor(structure)（eb487a3）把三个运行时默认路径（DB_PATH/COOKIE_FILE/BACKUP_DIR）从仓库根挪到 data/ 子目录，但代码里没有任何创建 data/ 目录逻辑、也无旧数据迁移——已有部署升级后启动即崩溃（`Cannot open database because the directory does not exist`），且根目录几十万条历史库被晾在一边（「数据假丢失」）
- 修复三件套：
  - `core/storage.js` Storage.init() 在 new Database 前新增 `mkdirSync(dirname(dbPath), {recursive:true})`（多级父目录自动建，幂等无副作用）
  - 新增 `core/migrate-legacy-data.js`：一次性迁移引导，检测根目录旧运行时文件（vrc-monitor.sqlite3[-wal|-shm]/auth_cookie.txt/backups/），仅当 data/ 下目标不存在时才搬移（防覆盖、幂等、失败 warn 兜底不阻断启动）
  - 评审补齐（复测采纳）：检测到根目录旧库存在但 data/ 已有同名库（用户手动 mkdir workaround 过的场景）时输出 warn 提示（防「假丢失」无从察觉）
- 回归测试：新增 `test/test-migrate-data.mjs`（Storage.init 无父目录不崩 + 迁移引导行为/warn，25 断言），接入 CI 冒烟
- 验证：test-migrate-data 25/25、test-registry PASS、check-doc-drift has_drift=false
- **演进意义**：**「运行时数据目录化」的迁移闭环补齐**——refactor 把运行时文件收进 data/ 是正确方向（gitignore 干净、部署清晰），但目录创建 + 旧数据迁移必须同步落地，否则升级=启动即崩 + 数据「假丢失」。确立模式：① **改默认/运行时路径必须同 commit 处理「目录存在性 + 旧数据迁移 + 回归测试」三件套**，否则是过渡态危害已有部署；② 迁移引导的防覆盖 + 幂等 + 失败兜底让升级零人工干预；③ 评审补的「静默跳过 warn」把「数据没动」从无从察觉变成可感知，是防「假丢失」的体验底线。

## 2026-08-26 · 修复 test/test-safe-mode.mjs 死引用（issue #106）

- `#106` (CLOSED, 2026-08-26) test/test-safe-mode.mjs 引用已删除的 core/mcp-definitions.js 致安全模式回归测试失效（修复 commit 59582d0）
- 背景：refactor(structure) 删除 core/mcp-definitions.js（工具定义拆为 core/tools/*.js，由 core/registry.js 聚合注册），test-safe-mode.mjs 仍 import 它 → ERR_MODULE_NOT_FOUND 直接崩溃，安全模式回归自动测试失效（main 上同样存在，非单 PR 引入）
- 修复：
  - CUSTOM_TOOLS 改用权威工具名清单 core/tool-order.json 的 tool_order 构造（filterTools/无漂移断言仅依赖 name，语义一致，tool_order 涵盖核心+插件全部工具）
  - e2e「普通工具仍可用」样例 get_boop_emojis → 核心离线工具 get_server_status（补最小 ctx 桩 storage/serverState）
  - DESTRUCTIVE_TOOLS 可见性断言仅针对「当前已注册」破坏性工具（registry.getRegistryMap），避免插件工具未加载而误报
- 验证：node test/test-safe-mode.mjs 全部断言通过；check-doc-drift has_drift=false
- **演进意义**：**架构重构的「测试配套迁移」不能滞后**——拆分/删除核心模块时必须同步修正引用它的测试，否则回归防线形同虚设。确立模式：删除模块前 grep 全仓引用（含 test/、scripts/、CI），测试数据源优先用「权威清单」（tool-order.json）而非「被拆的单一模块」（mcp-definitions.js）。

## 2026-08-26 · 发布 v3.1.0

**发布**：工具数 92 → 102（+10）。核心是 favorites 域收藏夹管理补全（含 VRC+ 专属收藏夹支持与整理闭环）+ auth-guard 公网鉴权/Docker 容器化 + events 社区活动聚合插件 + data/ 目录化迁移闭环 + 测试配套修复。

**关键改动**（相对 v3.0.0）：
- **favorites 收藏夹管理补全（#99/#100/#101/#105，@CyberNekokoya）**：动态分组发现（world + vrcPlusWorld）取代硬编码 FAVORITE_TAGS、按收藏时间排序（sortBy=added，利用列表天然倒序）、/worlds/favorites 一次拉全（首调 15-20min 降至秒级）、新增 unfavorite_world / move_world_group / update_favorite_group / clear_favorite_group 四工具、capacity 改自 favoritelimits —— VRC+ 专属收藏夹从不可见不可写到完整管理（+4 工具）
- **auth-guard 公网鉴权插件 + Docker 容器化部署（#98，@psenY）**：Bearer/X-API-Key/?token 三方式令牌提取 + crypto.timingSafeEqual 常量时间比对防侧信道；未配置 Token 时默认放行（本地零配置体验不变）；docker-compose 一键部署补全 NAS/路由器场景
- **events 社区活动聚合官方插件（#107，@268500tao）**：多源聚合 VRChat 各社区（中文/日文/韩文/国际）近期活动，按音乐/DJ/VTuber 偏好筛选，按群组热度/区域/时区整理成可读数据；3 个 MCP 工具（fetch_community_events / get_community_events_config / set_community_events_google_key），多源采集 + 三级群组深挖 + 双列时区标准化，使用者 Google API Key 存于本地数据库 `plg_events_config`（不随代码进入 Git 仓库、查询接口不回显明文）（详见本文件 2026-08-27 events 独立条目）
- **Windows 常驻服务静默化（#97，@psenY）**：pythonw.exe 探测优先，解决计划任务每分钟控制台窗口闪烁
- **data/ 目录化迁移闭环（#104，维护者）**：Storage.init 自动建目录 + migrate-legacy-data 一次性迁移引导 + 回归测试，修复升级启动崩溃 + 历史数据「假丢失」（issue #103）
- **测试配套修复（#106）**：test-safe-mode.mjs 死引用 —— 安全模式回归防线恢复
- **chore（#102，@CyberNekokoya）**：注册仓库 skills/ 为项目级 skills

**演进意义**：**从「架构转身」进入「功能补全 + 部署形态完备」的 3.1 阶段**——v3.0.0 确立插件化骨架，3.1.0 验证插件化贡献模型的实战产出（auth-guard 是贡献模型冻结解除后第一个完整走完「插件 + 底座扩展 + 文档登记 + 审查修复」流程的外部大功能；favorites 三连 + VRC+ 支持是插件优先的收藏域深耕），并补齐部署三大形态（Linux systemd / Windows pythonw / Docker 容器）与运行时目录化迁移。确立模式：**架构大版本之后的第一个 minor 通常是「骨架验证期」——外部贡献产出 + 部署完备 + 迁移闭环 + 回归防线恢复**，v3.1.0 的定位即此。
## 2026-08-27 · events 官方插件——VRChat 社区活动聚合（PR #107 @268500tao，已合并）

- `#107` (MERGED, 2026-08-27) feat(plugins): 新增 events 官方插件——VRChat 社区活动聚合（作者 @268500tao）
- 需求来源（使用者提出）：把 VRChat 各社区（中文/日文/韩文/国际）的近期活动聚合起来，按音乐 / DJ / VTuber 偏好筛选，并按群组热度、所在区域与时区整理成可读数据（供 Agent 消费或渲染 PDF）；此前在本地 fork 用若干独立脚本实现，本次整合、固化为官方插件回馈上游
- 实现（`plugins/official/events/`，零外部依赖，仅 Node ≥22 内置 fetch，经 `register(api)` + `api.registerTool` 暴露）：
  - **3 个 MCP 工具**：`fetch_community_events`（多源采集 → 群组深挖 → 音乐∪VTuber 筛选 → 结构化 JSON）、`get_community_events_config`（查看 Google API Key 配置状态）、`set_community_events_google_key`（录入/清除使用者自己的 Google API Key，存 `plg_events_config` 不回显）
  - **多源采集**：VRC Search（全球 SSR）、RLVRC（中文 JSON）、VRCEve / VRCEvent-KR（日/韩 Google Calendar API）
  - **三级群组深挖**：短码 `/groups/redirect/{sc}` 302 反查 → 活动名关键词 `GET /groups?query=`（Jaccard>0.45 + 质量门槛防误配）→ 描述里的借用群组/世界名；已有关联群组的补 `member_count` 热度 + icon
  - **侧补充源**：`peekGroups=true` 时窥探已挖掘群组的公告解析活动线索（复用核心 `peek_group_announcement`，默认关，有副作用）
  - **数据后处理标准化**（`enrichEvent` 每个返回事件自动注入）：规范图片 URL（file 单 `/file` 消除重复后缀）、双列时区（`start_local`/`start_bj`/`tz_label`/`tz_offset`，naive 按 languages 判社团时区、aware 读自带偏移）、中文参加方式（`join_info_zh` 由 VRCEve 日文【参加方法】规则化）、`category_zh`
  - **DB**：`plg_events_store`（事件缓存）+ `plg_events_config`（key 键值存储），幂等迁移
- 文档同步：工具登记进 `core/tool-order.json`、`skills/vrc-monitor-agent/SKILL.md` 权威清单、README / AGENTS.md / docs/PLUGIN-DEV.md 采样；插件自带 `SKILL.md` + `smoke-events.mjs`
- 验证：`test-registry.mjs` PASS（98 tools）；`dump-tools.mjs` 98 个与 tool-order 动态对齐；`smoke-events.mjs` PASS；`check-doc-drift.py --json` `missing_in_readme: []`（has_drift 仅剩上游 #97 状态漂移，与本 PR 无关）；真实 MCP 调用采集 1985 条原始活动，返回带 `start_local`/`start_bj`/`tz_label`（naive→按 language 判时区如 ET/JST，aware VRCEve→JST）、规范图标 URL、中文参加方式；`start-monitor.js` 启动正常 `/health` auth:true
- 无个人环境硬编码、无密钥提交（Google API Key 为用户录入存 DB，不随代码；未配置时 VRCEve/VRCEvent-KR 优雅跳过，VRC Search + RLVRC 照常工作）
- **演进意义**：**「跨社区活动聚合」从个人脚本到官方插件的固化，且是首个明确面向「Agent 消费 / PDF 渲染管道」输出的数据管道插件**——使用者要的不是又一个查询工具，而是「聚合多社区、按偏好筛选、按热度/区域/时区整理」的**成品数据**，此前靠 fork 里零散脚本，无法惠及所有使用者。确立模式：① **插件化贡献模型的第二条完整链路**（同 #98 auth-guard：本地 fork 实践 → 固化官方插件 → 文档登记 → 审查修复），插件契约 `register(api)` + `registerTool` 再次被外部验证；② **采集层纵深**：多源（SSR/JSON/Calendar API）+ 三级群组深挖（短码反查 → 关键词匹配 → 描述借用）+ 侧补充源（公告窥探），单一数据源失效不致命；③ **输出标准化是插件对下游的契约**——双列时区（start_local/start_bj）、中文参加方式、category_zh 统一注入，让渲染/Agent 消费端无需关心源差异；④ **使用者密钥本地化**：Google API Key 用户录入存 DB 不回显、不随代码提交，延续「个人化数据不入仓库」原则。

## 2026-08-27 · events 插件代理作用域修复（维护方补丁，commit 88bdb12）

- `88bdb12` fix(events): 修复插件代理作用域遮蔽——采集函数改用模块级代理 httpGet（PR #107 合并后维护方复测补丁）
- 背景：作者在插件里新增了模块级 `httpGet`（走 `tryFetchWithProxy`/`HttpsProxyAgent` 代理），但 `register()` 内的采集函数并未调用它——被内层旧 `fetch()` 版 httpGet **作用域遮蔽**，导致日/韩 Google Calendar 源实际未走代理
- 修复：删除 `register()` 内旧 fetch 版 httpGet，使 `collectVrcSearch`/`collectRlvrc`/`collectGoogleCalendar` 等解析到模块级代理版（1 file，-17 行）
- 验证（真实 Google key 实测）：日/韩 Google Calendar 源经代理正常抓取（日本 collected 1248 / 韩国 24 条真实活动），韩国活动 `tz_label` 正确为 KST（不再误标 JST）
- **演进意义**：**「合并 ≠ 完成」——外部 PR 合入后维护者必须真实环境复测**。本补丁暴露两层坑：① JS 作用域遮蔽是插件/模块组织的隐性陷阱——模块级同名函数与闭包内局部函数并存时，闭包内优先解析到局部版，作者写的「代理版」被「旧 fetch 版」静默遮蔽，代理白配；② 真实数据仍有边界情况（韩区活动 naive 时区被误判为 JST），需要真实 key 实测兜底。确立模式：**外部功能合入后「维护方复测 + 真实凭据实测 + 时区等边界字段复核」是质量闭环的最后一步**。

## 2026-08-28 · 修复 events 插件 CodeQL 安全告警（维护者修复，commit 0425218）

- `0425218` fix(events): 消除 CodeQL 安全告警——单遍实体解码/标签清洗加固/全局转义（无 PR 直接提交，作者 超级有节操的逆袭）
- 背景：GitHub CodeQL 安全扫描（2026-08 起接入开发流）对 events 插件报 3 条告警，全部指向 PR #107 新增代码的 HTML/实体处理链：
  - `js/double-escaping`（#15）：`decodeEntities()` 链式 replace 先解 `&amp;→&` 再解其它实体 → 双重反转义（如 `&amp;lt;` 被解两次变成 `<`）
  - `js/incomplete-multi-character-sanitization`（#16）：`stripHtml()` 先删标签再解码 → `&lt;script&gt;` 与无闭合 `<script` 可绕过清洗形成注入
  - `js/incomplete-sanitization`（#17）：langs 解析 `.replace('<','')` 只替换首个 `<`，残留后续 `<` 字符
- 修复（`plugins/official/events/index.js`，1 file +10/-3）：
  - `decodeEntities()` 改为**单遍正则 `&(#?\w+);` + 白名单映射解码**（amp/lt/gt/quot/#39/apos/#x27），一次扫描完成，杜绝双重反转义
  - `stripHtml()` 改为**先解码实体 → 删完整标签 → 末后中和残留 `<`/`>`**，堵住 `&lt;script&gt;` 与无闭合 `<script` 两类注入
  - langs 解析 `.replace('<','')` → `.replace(/</g,'')` 全局替换
- 验证：sanitize 用例 + `smoke-events.mjs` PASS，插件注册/dispatch 正常
- **演进意义**：**「CodeQL 安全扫描进入日常闭环」——PR #107 的功能性合并 ≠ 安全收尾**。events 插件是首个大量抓取第三方 SSR/HTML 页面的插件（VRC Search/RLVRC 卡片解析），HTML 实体/标签清洗是该类采集管道的固有攻击面；三条告警全部落在同一函数簇（decodeEntities/stripHtml/langs 解析），说明安全扫描确实在「新增代码」上生效、发现即修。确立模式：① **实体解码用「单遍正则 + 白名单映射」而非「链式 replace」**——链式 replace 天然存在双重解码窗口（`&amp;lt;` 两次替换变 `<`），是 html 解码的通用反模式；② **清洗顺序「先解码后删标签 + 末尾中和残留尖括号」**封死编码绕过（`&lt;script&gt;`）与残缺标签绕过（无闭合 `<script`）两路；③ 采集类插件合并后应主动复查安全扫描结果，勿等告警积累。

## 2026-08-30 · Web Dashboard 全家桶——核心数据服务下沉 + 非好友追踪 + 可视化前端 + Agent 技能集（PR #108/#109/#110/#111 @psenY）

- `#108` (CLOSED, 2026-08-30) feat(core): 核心数据服务下沉 + 非好友追踪闭环 + 回归测试（871cc73 等）
- `#109` (CLOSED, 2026-08-30) feat(dashboard): Web Dashboard 完整实装——24 视图 + 媒体/素材/公告 + 通知中心 + UI 统一（7cb4ec4）
- `#110` (CLOSED, 2026-08-30) docs+ops: Dashboard 文档体系（路由表/开发记录/交接）+ 运维检查脚本（bdabf91）
- `#111` (CLOSED, 2026-08-30) feat(skills): Agent 技能集——开发/审核/上下文管理等工作流 skill（独立提交，a93c2d8）
- 流程：四个子 PR 各自逐 PR 审核 APPROVE 后，组装进暂存分支 dashboard-series，统一经汇总 PR #112 合入 main（合并顺序约束 #108→#109→#110→#111 已验证）
- **#108 核心**：新建 core/dashboard-services.js——dashboard.* 服务族（tracked / trackedChanges / weeklyReport / recommendWorlds / communityEvents / groupAnnouncements / worldHistory / recentWorlds / stats）owner=core；**非好友追踪闭环**——每小时快照 diff → 变化事件入库（tracked_non_friends 表，幂等 PRAGMA+ALTER 迁移），selfId 四层防护防自追，removed_at 过滤刷新；新增 MCP 工具 **get_recent_cooplay**（最近一起玩：面向自己的全好友批量同屏版，days/limit 可调，口径与 get_weekly_report 一致，与 get_friend_pair_screen 两人版互补）；补 core/ops-log.js（recordOpsLog/setOpsLogSink 运维日志通道）
- **#109 前端**：plugins/official/web-dashboard 插件——Vue 3.5 SPA（1.45MB，24 视图 + 媒体/素材/公告 + 通知中心 + UI 统一），/dashboard + /api/dashboard/* 数据端点；core 侧补 api.http.registerRoute 契约（插件 HTTP 路由注册/分发/卸载清理）
- **#110 文档+运维**：Dashboard 路由表/开发记录/交接文档 + 运维检查脚本（api-live-audit / dashboard-probe），全程去敏个人环境信息
- **#111 技能集**：skills/ 扩充至 20 个与本仓库直接相关的工作流 skill（PR 审核、需求→验收闭环、plugin-first-web-dashboard 等），移除 21 个无关通用方法论
- 验证（端到端实测）：隔离端口真实启动——web-dashboard 插件 loaded、Vue 前端完整渲染、数据端点返回真实 VRChat 数据；单测 dashboard-util 9/9、test-dashboard-services 12/12、registry 103 工具、safe-mode 8、totp 27、migrate-data 25、storage-snapshot 62；check-doc-drift has_drift=false；敏感信息扫描净
- **演进意义**：**项目从「纯 MCP 工具服务」走向「MCP + Web 可视化 + Agent 技能体系」三形态**——v3.1.0 确立插件化骨架后，本系列是首个大型跨层全家桶：核心数据服务下沉（dashboard.* 服务族）让同一份逻辑同时喂 MCP 工具、Web 前端与插件，避免各消费端各写一套；非好友追踪把监控对象从「好友」扩展到「任意追踪账号」，补齐监控语义闭环；skills/ 从「查询工作流」扩展到「开发/审核/交付方法论」——Agent（而非人类）成为主要开发力量后，仓库内沉淀 Agent 协作规范是自然演化。确立模式：**大规模功能按「核心→前端→文档→技能」拆成堆叠 PR 家族，逐 PR 审核后再组装合入**。

## 2026-08-30 · 汇总 PR #112 + 评审 R1/R2/R3 阻断项修复 + identical-changes 合并静默丢修复陷阱

- `#112` (MERGED, 2026-08-31, #108/#109/#110/#111 汇总) feat(dashboard): 核心数据服务下沉 + 非好友追踪 + Web Dashboard 全家桶（@nixi-agent，head=dashboard-series → main）
- 汇总 PR 定位：把四个已各自 APPROVE 的子 PR 组装到暂存分支后**一次性合入 main**，团队在汇总 PR 上做 final review；合并顺序约束已验证（#108→#109→#110→#111）
- 评审阻断项修复（合入前清零）：
  - **R1 个人环境信息出清**：移除私有部署交接文档 HANDOFF/PR-MATERIALS（个人环境信息）、移除含路由器路径硬编码的本地环境脚本（deploy/backup/verify）、移除误提交的 .agents 工作区（本地 agent 状态/技能/图片，522f356）
  - **R1 跨 PR 重复文件只保留一个归属**：check-doc-drift.py 恢复为 upstream 原版——UTF-8 修复只归 #108 一处（75258ff；同文件多 PR 各改一份 → 合并期互相吞噬）
  - **R2 合并即崩阻断**：补 api.http.registerRoute 契约（web-dashboard 加载即崩，829d9be）、core/ops-log.js（recordOpsLog/setOpsLogSink，合并后启动即崩，2f14a9f）、tracked_non_friends 建表（全新库 CI no such table，8c88c6f）
  - **R3 范围控制**：移除 21 个与本仓库无关的通用 skill（评审 R3：范围外夹带，bb880b6）+ openwrt-public-exposure（个人环境硬编码，安全隐患，d4d6c10）
  - 新工具 get_recent_cooplay 登记三处权威清单：tool-order.json + SKILL.md 工具表 + AGENTS.md（8fede07/c9cdddd/c6e2afa，评审警告项）
- **identical-changes 合并静默丢修复陷阱**（9589094，2026-08-31 补回）：check-doc-drift.py 的 Windows UTF-8 输出修复（cp1252 打印中文 UnicodeEncodeError 崩溃）先随 #108 提交；汇总至 dashboard-series 时，因 #111-v2 存在**内容完全一致的相同改动**，git 合并自动消解（identical changes 互相吞并），修复在汇总分支上丢失 → Windows CI 复崩 → 需 HEAD 9589094 补回（此前 8fe74f8 已因同因保留过一次）
- **演进意义**：**「堆叠 PR 家族 → 暂存分支 → 汇总 PR」流程的实战教训集**：① **identical changes 是合并静默丢修复的元凶**——两个 PR 各自带着同一份修复（如 CI 脚本补丁）合入同一目标分支时，git 判定内容一致自动消解，任何一方的「后续演进版」都可能被另一方覆盖；合并后必须 diff 校验目标分支该文件确实含最新修复（本案 check-doc-drift.py UTF-8 补丁丢失两次才在 HEAD 补回，与 memory 中记录的 identical-changes 陷阱完全对应）；② **跨 PR 重复文件只保留一个归属**（评审 R1 阻断项 2），否则同文件多份改动在合并期互相吞噬；③ 个人环境信息（IP/路径/凭据/交接文档）是 PR 审核的第一道闸，R1（信息出清）→ R2（可运行性）→ R3（范围控制）依次清障是堆叠家族的固定审核顺序；④ 汇总 PR 的 body 承载「合并顺序约束 + 各子 PR 审核结论 + 端到端验证摘要」，让 final review 一次性完成。
